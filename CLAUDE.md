# אתר אהרוני שיווק והפצה (aroam.co.il) — מסמך הקשר לעבודה עם Claude

> קובץ זה נקרא אוטומטית בתחילת כל שיחה. הוא מסכם את העסק, מבנה האתר, מה בוצע ומה נשאר פתוח.
> עדכן אותו בסוף כל סבב עבודה משמעותי.

## העסק

- **אהרוני שיווק והפצה** — ספק B2B סיטונאי: חד פעמי, חומרי ניקיון, מוצרי נייר, ציוד משרדי ועוד.
- לקוחות: מפעלים, מוסדות, משרדים, מסעדות. אזור פעילות: אור יהודה, בקעת אונו, גוש דן והמרכז.
- בעלים: רועי אהרוני. טלפונים: 052-6000158 (נייד/וואטסאפ), 03-6346236 (משרד), 050-6444290. מייל הזמנות: meiraroam@gmail.com.
- **זהות מותג: גווני ירוק פסטל — לשמור עליה בכל שינוי עיצובי.** (primary ‎#639C7D, כהה ‎#1A4231, פסטל ‎#8FD6A3)

## ארכיטקטורה

אתר סטטי (HTML/CSS/JS, בלי framework) בעברית RTL. מאוחסן ב-**GitHub Pages** (ריפו: github.com/roeiaharoni4/aroan, דומיין aroam.co.il דרך קובץ CNAME). פריסה = `git push` ל-main.
**מיקום פעיל (12.7.26):** `קטלוג לקוחות/aroan` — זהו הריפו החי. כל שדרוגי יולי 2026 + העיצוב החדש הועברו לכאן מ"עותק של aroan", עם products.csv מאוחד (177 שורות) ותיקון 8 נתיבי תמונות (`:` → `-`). התיקיות "עותק של aroan" ו-aroam/aroam-catalog הן עותקי עבודה ישנים — לא לעבוד בהן. (WORDPRESS_DEPLOY.md מיושן — האתר כבר לא בוורדפרס.)

- **מקור נתוני מוצרים:** `data/products.csv` (עמודות: id, name, category, unit, image, price, description; עמודת brand אופציונלית — גוברת על זיהוי אוטומטי). אפשר לחבר ל-Google Sheets מפורסם (ראה UPDATES_GUIDE.md).
- **js/app.js** — לוגיקת הקטלוג (טעינת CSV עם PapaParse, קטגוריות, חיפוש, מותגים, עגלה, שליחת הזמנה בוואטסאפ/מייל, הזמנות אחרונות, פרטי לקוח, אישור שליחה). משותף לקטלוג הלקוחות, דפי הקטגוריה ודף הסוכן. יש פרמטר גרסה בקישורים (`app.js?v=15`) — להעלות מספר אחרי כל שינוי.
- **js/components.js** — הדר/פוטר (custom elements), וואטסאפ צף, טפסי לידים (AJAX ל-Google Apps Script), מעקב המרות (dataLayer).
- **js/catalog-shell.js** — רכיב `<catalog-shell>` שמזריק את ממשק הקטלוג לדפי הקטגוריה. ה-IDs חייבים להישאר זהים ל-catalog/index.html.
- **css/site.css** — עיצוב האתר הכללי (משתני צבע + מצב כהה). **css/style.css** — עיצוב אפליקציית הקטלוג.
- **דפים:** דף בית, `/catalog/` + 10 דפי קטגוריה סטטיים ל-SEO (cleaning, paper, disposables, office-supplies, cleaning-accessories, hygiene, bags, air-fresheners, crafts, misc — כל אחד עם presetCategory), דפי נחיתה לפלחים (`/offices/`, `/restaurants/`, `/factories/`, `/education/`), אודות, צור קשר, 404.html, sitemap.xml.
- **אזורים רגישים — לא לשבור:**
  - `/admin/` — עורך מוצרים מקומי. עובד רק דרך `server.py` (רץ מ-start_catalog.command) עם API‏ `/api/products` ו-`/api/upload`. לא עובד באתר החי.
  - `/agent/` — גרסת סוכן עם מחירים (קוד כניסה 0526), משתמש באותו app.js עם CATALOG_CONFIG שונה (showPrices: true). אין בו טופס פרטי לקוח — הקוד ב-app.js בנוי לדלג כשאין את השדות.
- **Google Apps Script:** webhook הזמנות (GOOGLE_SCRIPT_WEBHOOK_URL ב-app.js שורה 3) + webhook טפסי לידים (ב-components.js). קוד משודרג לגיליון הזמנות עם סטטוס: `tools/google-apps-script-orders.gs` (ייתכן שעדיין לא הותקן — לבדוק עם רועי).
- **GTM:** GTM-W8HST8RB בכל הדפים. אירועי dataLayer: whatsapp_click, phone_click, form_submit, order_sent. חיבור ל-GA4 — באחריות רועי (ייתכן שטרם בוצע).

## תהליכי עבודה

- **עריכת מוצרים:** להריץ start_catalog.command ← localhost/admin ← שמירה מעדכנת את ה-CSV המקומי ← commit+push לפרסום.
- **בדיקה מקומית:** start_catalog.command (שרת על פורט 8080-8090). סגירת החלון מכבה את השרת.
- **גרסאות:** יש git בתיקייה. לפני כל סבב שינויים: `git add -A && git commit -m "תיאור"`. שחזור: `git reset --hard HEAD`.
- **תמונות מוצרים:** `tools/עדכון_תמונות.command` — מאחד רקע לבן/ריבוע/הקטנה, מגבה ל-`images_backup/`.
- **פריסה:** `git add -A && git commit && git push` — GitHub Pages מתעדכן אוטומטית תוך דקות. אחרי שינויי SEO — לשלוח sitemap מחדש ב-Search Console.

## מה בוצע (יולי 2026)

1. **SEO:** 10 דפי קטגוריה סטטיים עם מטא, סכמה (CollectionPage+ItemList+Breadcrumb) וטקסט; דפי נחיתה מפעלים/מוסדות חינוך; sitemap.xml מעודכן; קישורים פנימיים בדף הבית ובפוטר.
2. **המרה:** כפתורי וואטסאפ/טלפון בהירו; טופס פרטי לקוח חובה בהזמנה (שם עסק+טלפון, נשמר במכשיר); חלון אישור אחרי שליחה עם מספר הזמנה; שורת "המותגים המובילים" בדף הבית (סנו, יעקובי, קימברלי קלארק, פיירי, פיניש, דטול, זוהר דליה).
3. **קטלוג:** קטגוריית "הזמנות אחרונות" (נשמרות 5 אחרונות במכשיר, מופיעה רק אם יש); סינון לפי מותג (זיהוי אוטומטי מהשם, KH7 שייך ליעקובי, בלי מותגי היגיינה); בחירת מותג מציגה את כל הקטגוריות.
4. **עיצוב:** אימוג'ים הוחלפו באייקוני SVG; פונטים אוחדו (Assistant+Heebo); פלטת הקטלוג יושרה למותג; הירו בגרדיאנט ירוק במקום תמונת סטוק; כפתורי פעולה בירוק המותג; דף 404.
5. **תפעול:** מעקב המרות dataLayer; קוד Apps Script לגיליון הזמנות; סקריפט תמונות.
6. **GEO (9.7.26):** robots.txt עם היתר מפורש לסורקי AI + חסימת /admin/ ו-/agent/; llms.txt הורחב (כל דפי הקטגוריה והנחיתה, דו-לשוני); תיקוני alt (תצוגה מהירה ב-app.js, גרסה הועלתה ל-v=16; אייקוני דף הבית). רועי דחה דף שאלות נפוצות. מוצע ולא בוצע: פסקאות פתיחה ישירות בדפי קטגוריה. רועי רשום ב-easy וב-012/B144. git לא עובד מסביבת Claude (Resource deadlock) — רועי מריץ commit+push בעצמו.

7. **ביצועים (10.7.26):** תמונות דף הבית ודפי הנחיתה (הירו, קטגוריות, אייקונים) הומרו ל-WebP והוקטנו לגודל תצוגה (‎415KB→44KB, קובצי ה-PNG המקוריים נשארו בתיקייה); עודכנו הפניות ב-index.html, ‏4 דפי נחיתה ו-app.js (גרסה v=17 בכל הדפים); preload להירו עם fetchpriority=high; הוסרו משקלי פונט 300 שלא בשימוש (בדפים שטענו אותם); sw.js הועלה ל-cache-v2. לא בשליטה: cache קצר של GitHub Pages ו-GTM. בנוסף: llms.txt הומר לקישורי Markdown ‏(`[שם](כתובת)`) בעקבות בדיקת Agentic Browsing של PageSpeed שדרשה את זה.

**הערה:** רועי עשה `git reset` אחד אחרי הריענון העיצובי ואז ביקש החזרות נקודתיות — המצב הסופי: עיצוב הסינון פשוט (inline), KH7 תחת יעקובי. לוודא מול הקבצים בפועל ולא מול ההיסטוריה.

8. **שדרוג עיצובי B2B (12.7.26):** נקודת שחזור: `272333d`. דף הבית — שורת utility עליונה (רק טלפון משרד 03-6346236 + אזור חלוקה, בלי שעות ובלי הנייד; ב-components.js), הירו דו-טורי בגרדיאנט ירוק כהה עם כרטיסיית עובדות בלי מספרים, פינות 6-10px, הוסרו hover-transforms. קטלוג — style.css יושר לירוק המותג (‎#639C7D, היה טורקיז ‎#00897b); עמודת קטגוריות בצד בדסקטופ ≥1024px (nav#categories הוזז ל-aside.catalog-sidebar ב-catalog/index.html + catalog-shell.js, מסומן ב-.app-container.with-sidebar — דף הסוכן נשאר במבנה הישן בכוונה); מתג תצוגת רשת/רשימה (מוזרק מ-components.js ל-.search-container, class ‏list-view על #products, נשמר ב-localStorage‏ aroam_catalog_view); כפתור ההזמנה הצף הפך לבר תחתון קבוע ברוחב מלא (.fab-container), וואטסאפ צף/גלילה-למעלה הורמו עם ‎:has. **אפס שינויים ב-app.js.** גרסאות: style.css?v=12, sw cache-v3. הכל אומת בדפדפן כולל עגלה+מודאל בדסקטופ ומובייל.

9. **סבב תיקונים (13.7.26):** נקודת שחזור: `7501f79`. **אבטחה:** הסיסמה Aroam2026 הוסרה מ-app.js ומ-agent/backend_script.js (שניהם מוגשים פומבית באתר) — היסטוריית ההצעות בדף הסוכן שואלת עכשיו קוד גישה פעם אחת ושומרת ב-sessionStorage בלבד; קוד כניסת הסוכן הוחלף מ-0526 ל-**0526000158** (הנייד המלא); tools/google-apps-script-orders.gs חוזק (ניקוי קלט, מניעת formula injection בגיליון, הגבלת 30 הזמנות/שעה) — עדיין ממתין להתקנה אצל רועי. **תוקן באג:** `openOrderModal()` נקרא ב-loadQuote (דף סוכן) אך לא היה מוגדר — ReferenceError בטעינת הצעה מהיסטוריה; הוגדר ב-app.js. **מובייל:** כפתורי מועדפים/תצוגה מהירה גלויים במגע (`@media (hover:none)` ב-style.css; בתצוגת רשימה נשארו מוסתרים בכוונה); כפתור הנגישות והתפריט הורמו מעל בר ההזמנה (`body:has(.fab-container)` ב-accessibility.css); נעילת גלילת רקע בכל המודאלים (syncModalScrollLock ב-app.js). **שונות:** אייקון הוואטסאפ הצף הוא SVG מוטמע (היה נטען מ-wikimedia); דף הסוכן — כל האמוג'ים הוחלפו ב-SVG והטורקיז ‎#00897b בירוק המותג; determineImageBaseUrl מחזיר '/' קבוע (בוטלו 5 בדיקות רשת בכל טעינה). תמונת אבי004 קיימת ונטענת — לא נדרש תיקון. גרסאות: app.js?v=18, style.css?v=13, accessibility.css?v=4, sw cache-v4. אומת בדפדפן: דף בית, קטלוג (רשת+רשימה), דף קטגוריה, דף סוכן (PIN ישן נדחה/חדש עובד, מודאל), מובייל 375px.

10. **SEO — סבב אוטומטי (13.7.26, הוחל על הריפו החי aroan):** סכמת `LocalBusiness` ל-`/contact/` + `AboutPage/Organization` ל-`/about/` (כולל Open Graph שלא היה); תמונת OG ממותגת `images/og-image.png` (1200×630) והחלפת כל הפניות og:image מ-logo.png אליה (19 עמודים); **דף עיר חדש `/or-yehuda/`** (schema WebPage+Breadcrumb+LocalBusiness) מקושר מהפוטר, מדף הבית, מ-llms.txt ומ-sitemap; `sitemap.xml` — lastmod ל-2026-07-13 + or-yehuda (19 URLs). **בנוסף תוקן פער:** ה-`<title>` של דף הבית היה עדיין הישן ("הבית של מוצרי הניקיון") ושודרג ל"ספק סיטונאי לחומרי ניקיון, נייר וחד פעמי לעסקים | אהרוני". כל ה-JSON-LD אומת. **לא נדחף ל-git — צריך commit+push.** דורש רועי: אימות אינדוקס + הגשת sitemap ב-Search Console, הקמת GBP, sameAs.

11. **סבב חיזוק "חד פעמי" ו"ציוד משרדי" (13.7.26, על בסיס נתוני GSC):** ניתוח GSC (אפר–יול 26) הראה שכמעט כל ההופעות הן על "שיווק חומרי ניקוי"; לדף disposables היו 0 הופעות ול-office-supplies ‏2 הופעות במקום 32. מחקר מתחרים: גלניר/אור פעמי/סילקו (חד פעמי), אופיר ספקים/קותי/מיטב (ציוד משרדי) — כולם עם כותרות exact-match ו-800–1,500 מילים בדף. **בוצע (בלי שינוי נראות):** כרטיסיות הקטגוריות בדף הבית הוסבו מ-`/catalog/?category=...` לדפים הסטטיים (cleaning/paper/disposables); כותרות ותיאורי מטא חדשים ל-disposables ("חד פעמי בסיטונאות לעסקים | כלים חד פעמיים ואריזות"), office-supplies ("ציוד משרדי לעסקים | קטלוג ציוד משרדי במחירים סיטונאיים"), catalog ודף הבית (כולל og:title ישן שהוחלף); keywords בסכמות; צ'יפ קישור הדדי בין disposables↔office-supplies. אומת בדפדפן. **ממתין:** תוספות טקסט בדפי הקטגוריה (רועי דחה בינתיים — 14.7), אינדוקס ידני של שני הדפים ב-Search Console (רועי ביצע 14.7; ה-404 בדוח GSC הם /card ו-/digitalcard הישנים — לא לטפל).

12. **דפי נחיתה עירוניים (14.7.26):** רועי ביקש עוד דפי נחיתה במקום תוספות טקסט. נוצרו 6 דפי עיר מתבנית or-yehuda (סקריפט: scratchpad, חד-פעמי): `/yehud/`, `/kiryat-ono/`, `/ramat-gan/`, `/bnei-brak/`, `/petah-tikva/`, `/tel-aviv/` — כולם מובילים עם "חד פעמי" ו"ציוד משרדי" בכותרת וב-H1, תוכן ייחודי פר עיר (יהוד=קרבה, ק.אונו=מוסדות חינוך, ר"ג=מגדלי משרדים, ב"ב=מוסדות ואירועים, פ"ת=קריית אריה/סגולה, ת"א=אריזות למשלוחים), סכמת WebPage+Breadcrumb+LocalBusiness עם areaServed עירוני. קישורים: sitemap (25 URLs, lastmod 14.7), llms.txt, שמות הערים בפסקת דף הבית וב-or-yehuda הפכו לקישורים, צ'יפים הדדיים בין דפי הערים. הפוטר לא שונה בכוונה (לא להאריך את העמודה). אומת בדפדפן + JSON-LD. דורש רועי: בקשות אינדוקס ידניות לדפים החדשים ב-Search Console.



13. **תיקון הצעות חיפוש בדף הסוכן (15.7.26):** ההצעות בחיפוש הוצגו ענקיות וחסמו את המסך — עיצוב `.search-suggestions` היה רק ב-site.css שדף הסוכן לא טוען. הועתק ל-style.css (עם משתני style.css, כולל מצב כהה) ליד תיקון "האייקונים הענקיים" הקיים. גרסאות: style.css?v=14 (12 דפים), sw cache-v5. אומת בדפדפן בסוכן ובקטלוג. commit `3070c02` — ממתין ל-push.

- **להחליף את סיסמת ה-API בסקריפט היסטוריית ההצעות בענן** (Apps Script של AgentQuotes): הסיסמה Aroam2026 נחשפה בעבר בקבצים פומביים ובהיסטוריית git — לקבוע סיסמה חדשה בסקריפט, ובדף הסוכן להזין אותה כשנשאלים (נשמרת ל-session).
- התקנת `tools/google-apps-script-orders.gs` בחשבון גוגל (הוראות בראש הקובץ) — כולל עכשיו סינון קלט והגבלת קצב.
- חיבור GTM ל-GA4 (טריגרים לארבעת האירועים) + סימון Key Events.
- הרצת סקריפט התמונות ובדיקת התוצאה.
- לוודא שכל השינויים נדחפו ל-GitHub (push) + שליחת sitemap ב-Search Console.
- רעיונות שאושרו עקרונית אך לא בוצעו: "הוסף הכל לסל" בהזמנות אחרונות, מידע אריזה/קרטון למוצרים, מבצעי החודש, דף שאלות נפוצות (צריך תשובות מרועי), תמונת הירו אמיתית (צריך צילום), Google Business Profile.

## העדפות של רועי

- תשובות קצרות וענייניות, בעברית.
- בלי אימוג'ים בעיצוב האתר; כפתורים בסגנון האתר הקיים.
- לשמור על עיצוב פסטל ירוק; שינויים עיצוביים — רק עם נקודת שחזור git לפני.
- מוצרים בלי מחיר/תיאור זה מכוון — לא "לתקן".
