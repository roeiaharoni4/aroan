# אתר אהרוני שיווק והפצה (aroam.co.il) — מסמך הקשר לעבודה עם Claude

> קובץ זה נקרא אוטומטית בתחילת כל שיחה. הוא מסכם את העסק, מבנה האתר, מה בוצע ומה נשאר פתוח.
> עדכן אותו בסוף כל סבב עבודה משמעותי.

## העסק

- **אהרוני שיווק והפצה** — ספק B2B סיטונאי: חד פעמי, חומרי ניקיון, מוצרי נייר, ציוד משרדי ועוד.
- לקוחות: מפעלים, מוסדות, משרדים, מסעדות. אזור פעילות: אור יהודה, בקעת אונו, גוש דן והמרכז.
- בעלים: רועי אהרוני. טלפונים: 052-6000158 (נייד/וואטסאפ), 03-6346236 (משרד), 050-6444290. מייל הזמנות: meiraroam@gmail.com.
- **זהות מותג: גווני ירוק פסטל — לשמור עליה בכל שינוי עיצובי.** (primary ‎#639C7D, כהה ‎#1A4231, פסטל ‎#8FD6A3)

## ארכיטקטורה

אתר סטטי (HTML/CSS/JS, בלי framework) בעברית RTL. מאוחסן ב-**GitHub Pages** (ריפו: github.com/roeiaharoni4/aroan, דומיין aroam.co.il דרך קובץ CNAME). פריסה = `git push` ל-main.
**מיקום פעיל (12.7.26):** `קטלוג לקוחות/aroan` — זהו הריפו החי. כל שדרוגי יולי 2026 + העיצוב החדש הועברו לכאן מ"עותק של aroan", עם products.csv מאוחד (177 שורות) ותיקון 8 נתיבי תמונות (`:` → `-`). התיקיות "עותק של aroan" ו-aroam/aroam-catalog הן עותקי עבודה ישנים — לא לעבוד בהן. (WORDPRESS_DEPLOY.md מיושן — האתר כבר לא בוורדפרס.)

- **מקור נתוני מוצרים:** `data/products.csv` (עמודות: id, name, category, unit, image, price, description; עמודת brand אופציונלית — גוברת על זיהוי אוטומטי). אפשר לחבר ל-Google Sheets מפורסם (ראה UPDATES_GUIDE.md).
- **js/app.js** — לוגיקת הקטלוג (טעינת CSV עם PapaParse, קטגוריות, חיפוש, מותגים, עגלה, שליחת הזמנה בוואטסאפ/מייל, הזמנות אחרונות, פרטי לקוח, אישור שליחה). משותף לקטלוג הלקוחות, דפי הקטגוריה ודף הסוכן. יש פרמטר גרסה בקישורים — להעלות מספר אחרי כל שינוי. **הגרסאות הנוכחיות (21.8.26): `app.js?v=48`, `style.css?v=35`, `sw` cache-v42 — בכל 15 הדפים.** ‏`components.js` נטען **בלי פרמטר גרסה**, ולכן אחרי שינוי בו הדפדפן עלול להגיש עותק מהמטמון עד שה-cache של GitHub Pages מתחלף (בבדיקה מקומית: `fetch('/js/components.js', {cache:'reload'})`).
- **js/components.js** — הדר/פוטר (custom elements), וואטסאפ צף, טפסי לידים (AJAX ל-Google Apps Script), מעקב המרות (dataLayer).
- **js/catalog-shell.js** — רכיב `<catalog-shell>` שמזריק את ממשק הקטלוג לדפי הקטגוריה. ה-IDs חייבים להישאר זהים ל-catalog/index.html.
- **css/site.css** — עיצוב האתר הכללי (משתני צבע + מצב כהה). **css/style.css** — עיצוב אפליקציית הקטלוג.
- **דפים:** דף בית, `/catalog/` + 10 דפי קטגוריה סטטיים ל-SEO (cleaning, paper, disposables, office-supplies, cleaning-accessories, hygiene, bags, air-fresheners, crafts, misc — כל אחד עם presetCategory), דפי נחיתה לפלחים (`/offices/`, `/restaurants/`, `/factories/`, `/education/`), אודות, צור קשר, 404.html, sitemap.xml.
- **אזורים רגישים — לא לשבור:**
  - `/admin/` — עורך מוצרים מקומי. עובד רק דרך `server.py` (רץ מ-start_catalog.command) עם API‏ `/api/products` ו-`/api/upload`. לא עובד באתר החי.
  - `/agent/` — גרסת סוכן עם מחירים (קוד כניסה 0526), משתמש באותו app.js עם CATALOG_CONFIG שונה (showPrices: true). אין בו טופס פרטי לקוח — הקוד ב-app.js בנוי לדלג כשאין את השדות.
  - `/care/` — **קטלוג טיפוח והיגיינה B2C** (16.7.26; נפתח לגוגל 18.7.26): קטלוג עם מחירים לצרכן למוצרים נפרדים (data/pricelist.csv). **ציבורי ומאונדקס** (index,follow, canonical, ‏H1, ב-sitemap וב-llms.txt) אבל **בכוונה כמעט לא מקושר מתפריטי האתר** — כדי שלקוחות B2B קבועים לא ייתקלו בו בגלישה רגילה; ההגעה אליו: גוגל או קישור ישיר. (23.7.26: נוסף **קישור פנימי יחיד ודיסקרטי** מדף `/about/` — משפט טבעי עם עוגן "קטלוג מוצרי טיפוח והיגיינה במחירים לצרכן" — כדי לתת ל-/care/ מעט כוח-קישור פנימי בלי חשיפה בתפריט הראשי; זו הפשרה שרועי בחר מול הצורך ב-SEO של עמוד "יתום".) סכמת JSON-LD ‏(CollectionPage+ItemList+Product עם מחירים) מוזרקת אוטומטית ע"י `update_care_schema()` ב-server.py בכל שמירה מהעורך, בין הסימונים CARE_SCHEMA_START/END. מחירי העלות רק ב-data/pricelist-master.csv המקומי (ב-.gitignore — אסור לדחוף אותו). עריכה: localhost/admin/pricelist.html.
  - `/shops-4b7e2c/` — **קטלוג סגור ללקוח רשת חנויות** (21.8.26): שער קוד (`ACCESS_CODE` בראש הסקריפט בעמוד, כרגע `3160`) ואחריו בורר סניף בריבועים לבנים על רקע ירוק: **מרכזי קניות** (מרכז ← חנות) ו**חנויות רחוב** (לחיצה אחת). לחיצה אחת לכל שלב, בלי כפתור אישור. טוען את `data/products.csv` הרגיל ומסנן אותו לרשימת המזהים ב-`data/chain.json`, ומחיל ממנו גם `overrides` — **שם לתצוגה ומחיר לרשת פר מוצר**. **אין מחירון נפרד, ובעמוד לא מוצגים מחירים** — המחיר נכנס רק לפיילוד ההזמנה. הסניף נכתב ל-`cust-business` המוסתר ולכן מגיע לגיליון ולתעודת ה-PDF בלי שינוי בענן; קידומת ההזמנה `RS`. noindex, לא ב-sitemap/llms/robots, אפס קישורים אליו מהאתר. עריכה: localhost/admin/chain.html.
- **Google Apps Script — שלושה סקריפטים נפרדים בחשבון של רועי (לא להתבלבל!):**
  | מזהה פריסה | תפקיד | איפה בקוד |
  |---|---|---|
  | `AKfycbz_rRjFdy2MqP…` | **הזמנות** ("aroma order webhook") | app.js שורה 3 |
  | `AKfycbyasqqt8uTERc…` | היסטוריית הצעות (דף הסוכן) | app.js ~שורה 1746 |
  | `AKfycbxfp0qDK0ergy…` | טפסי לידים (צור קשר) | components.js |

  **סקריפט ההזמנות הותקן ופרוס (31.7.26):** [פרויקט Apps Script](https://script.google.com/home/projects/1kwSk_xA6hbvMRDeJerUfxYw1jKuLsOIfEv9Xmav52kRe7ZHqqDHt5_cV/edit) · גיליון [הזמנות אהרוני](https://docs.google.com/spreadsheets/d/1IidCbfHIGuifPdcaP9Fv7bO6TTTEbSMJnk_gtUcU-jE/edit) (`SPREADSHEET_ID` בשורה 10 של הסקריפט). המקור: `tools/google-apps-script-orders.gs`. **גרסה 5 פרוסה (20.8.26, 22:28)**; הגרסאות הקודמות שמורות (שחזור: לפריסה ← ניהול הפריסות ← עריכה ← בחירת גרסה). מזהה הפריסה `AKfycbz_rRjFdy2MqP…` לא השתנה מעולם — אין צורך לגעת ב-app.js. היסטוריית הגרסאות: 2 = פרטי לקוח בגיליון (31.7) · 3 = תעודת PDF וקישור להצעת מחיר (20.8) · 4 = לוגו ביחס הנכון ושורות סיכום בכיוון עברית · 5 = קישור להצעת מחיר גם בהזמנות מהקטלוג העסקי.
  **שתי מלכודות לפני כל עדכון של הסקריפט בענן (נצרבו 20.8):** (א) הקובץ ב-`tools/` מכיל `YOUR_SPREADSHEET_ID` כמציין מקום — הדבקה שלו כמות שהוא **משביתה את הכתיבה לגיליון**; המזהה החי הוא `1IidCbfHIGuifPdcaP9Fv7bO6TTTEbSMJnk_gtUcU-jE`. (ב) הקוד בענן אינו זהה בייט-לבייט לקובץ ב-`tools/` (פער ~185 תווים גם אחרי הסרת הערות, לא זוהה). **לכן מעדכנים בהחלפות מחרוזת נקודתיות דרך `monaco.editor.getModels()[0]`, אחרי ספירת מופעים — לא בהדבקת הקובץ המלא.**
  **טיפ לזיהוי הפרויקט הנכון בעתיד:** לפתוח את כתובת ה-webhook בדפדפן (GET → שגיאת `doGet`), ואז ההפעלה תופיע בראש [ההפעלות שלי](https://script.google.com/home/executions) עם קישור לפרויקט.
- **GTM + GA4:** GTM-W8HST8RB בכל הדפים + **gtag.js ישיר עם GA4 מזהה G-W1MRCCC3FS** (נכס properties/545698526, הוטמע 15.7.26). אירועי המרה: whatsapp_click, phone_click, form_submit, order_sent — נשלחים גם ל-dataLayer (GTM) וגם ל-GA4 דרך gtag('event').

## תהליכי עבודה

- **עריכת מוצרים:** להריץ start_catalog.command ← localhost/admin ← שמירה מעדכנת את ה-CSV המקומי ← commit+push לפרסום.
- **בדיקה מקומית:** start_catalog.command (שרת על פורט 8080-8090). סגירת החלון מכבה את השרת.
- **גרסאות:** יש git בתיקייה. לפני כל סבב שינויים: `git add -A && git commit -m "תיאור"`. שחזור: `git reset --hard HEAD`.
- **תמונות מוצרים:** `tools/עדכון_תמונות.command` — מאחד רקע לבן/ריבוע/הקטנה, מגבה ל-`images_backup/`.
- **מצב פריסה (21.8.26):** רועי דחף עד commit `8e1ed35` — כלומר סבבים 1-38 חיים באתר. **סבבים 39-40 committed וממתינים ל-push.** בענן: סקריפט ההזמנות בגרסה 5, מעודכן.
- **פריסה:** `git add -A && git commit && git push` — GitHub Pages מתעדכן אוטומטית תוך דקות. אחרי שינויי SEO — לשלוח sitemap מחדש ב-Search Console. **הערה (15.7.26):** מסביבת Claude ‏commit עובד, אבל ל-push אין credentials — רועי מריץ `git push` בעצמו.

## מה בוצע (יולי 2026)

1. **SEO:** 10 דפי קטגוריה סטטיים עם מטא, סכמה (CollectionPage+ItemList+Breadcrumb) וטקסט; דפי נחיתה מפעלים/מוסדות חינוך; sitemap.xml מעודכן; קישורים פנימיים בדף הבית ובפוטר.
2. **המרה:** כפתורי וואטסאפ/טלפון בהירו; טופס פרטי לקוח חובה בהזמנה (שם עסק+טלפון, נשמר במכשיר); חלון אישור אחרי שליחה עם מספר הזמנה; שורת "המותגים המובילים" בדף הבית (סנו, יעקובי, קימברלי קלארק, פיירי, פיניש, דטול, זוהר דליה).
3. **קטלוג:** קטגוריית "הזמנות אחרונות" (נשמרות 5 אחרונות במכשיר, מופיעה רק אם יש); סינון לפי מותג (זיהוי אוטומטי מהשם, KH7 שייך ליעקובי, בלי מותגי היגיינה); בחירת מותג מציגה את כל הקטגוריות.
4. **עיצוב:** אימוג'ים הוחלפו באייקוני SVG; פונטים אוחדו (Assistant+Heebo); פלטת הקטלוג יושרה למותג; הירו בגרדיאנט ירוק במקום תמונת סטוק; כפתורי פעולה בירוק המותג; דף 404.
5. **תפעול:** מעקב המרות dataLayer; קוד Apps Script לגיליון הזמנות; סקריפט תמונות.
6. **GEO (9.7.26):** robots.txt עם היתר מפורש לסורקי AI + חסימת /admin/ ו-/agent/; llms.txt הורחב (כל דפי הקטגוריה והנחיתה, דו-לשוני); תיקוני alt (תצוגה מהירה ב-app.js, גרסה הועלתה ל-v=16; אייקוני דף הבית). רועי דחה דף שאלות נפוצות. מוצע ולא בוצע: פסקאות פתיחה ישירות בדפי קטגוריה. רועי רשום ב-easy וב-012/B144. git לא עובד מסביבת Claude (Resource deadlock) — רועי מריץ commit+push בעצמו.

7. **ביצועים (10.7.26):** תמונות דף הבית ודפי הנחיתה (הירו, קטגוריות, אייקונים) הומרו ל-WebP והוקטנו לגודל תצוגה (‎415KB→44KB, קובצי ה-PNG המקוריים נשארו בתיקייה); עודכנו הפניות ב-index.html, ‏4 דפי נחיתה ו-app.js (גרסה v=17 בכל הדפים); preload להירו עם fetchpriority=high; הוסרו משקלי פונט 300 שלא בשימוש (בדפים שטענו אותם); sw.js הועלה ל-cache-v2. לא בשליטה: cache קצר של GitHub Pages ו-GTM. בנוסף: llms.txt הומר לקישורי Markdown ‏(`[שם](כתובת)`) בעקבות בדיקת Agentic Browsing של PageSpeed שדרשה את זה.

**הערה:** רועי עשה `git reset` אחד אחרי הריענון העיצובי ואז ביקש החזרות נקודתיות — המצב הסופי: עיצוב הסינון פשוט (inline), KH7 תחת יעקובי. לוודא מול הקבצים בפועל ולא מול ההיסטוריה.

8. **שדרוג עיצובי B2B (12.7.26):** נקודת שחזור: `272333d`. דף הבית — שורת utility עליונה (רק טלפון משרד 03-6346236 + אזור חלוקה, בלי שעות ובלי הנייד; ב-components.js), הירו דו-טורי בגרדיאנט ירוק כהה עם כרטיסיית עובדות בלי מספרים, פינות 6-10px, הוסרו hover-transforms. קטלוג — style.css יושר לירוק המותג (‎#639C7D, היה טורקיז ‎#00897b); עמודת קטגוריות בצד בדסקטופ ≥1024px (nav#categories הוזז ל-aside.catalog-sidebar ב-catalog/index.html + catalog-shell.js, מסומן ב-.app-container.with-sidebar — דף הסוכן נשאר במבנה הישן בכוונה); מתג תצוגת רשת/רשימה (מוזרק מ-components.js ל-.search-container, class ‏list-view על #products, נשמר ב-localStorage‏ aroam_catalog_view); כפתור ההזמנה הצף הפך לבר תחתון קבוע ברוחב מלא (.fab-container), וואטסאפ צף/גלילה-למעלה הורמו עם ‎:has. **אפס שינויים ב-app.js.** גרסאות: style.css?v=12, sw cache-v3. הכל אומת בדפדפן כולל עגלה+מודאל בדסקטופ ומובייל.

9. **סבב תיקונים (13.7.26):** נקודת שחזור: `7501f79`. **אבטחה:** הסיסמה Aroam2026 הוסרה מ-app.js ומ-agent/backend_script.js (שניהם מוגשים פומבית באתר) — היסטוריית ההצעות בדף הסוכן שואלת עכשיו קוד גישה פעם אחת ושומרת ב-sessionStorage בלבד; קוד כניסת הסוכן הוחלף מ-0526 ל-**0526000158** (הנייד המלא); tools/google-apps-script-orders.gs חוזק (ניקוי קלט, מניעת formula injection בגיליון, הגבלת 30 הזמנות/שעה) — עדיין ממתין להתקנה אצל רועי. **תוקן באג:** `openOrderModal()` נקרא ב-loadQuote (דף סוכן) אך לא היה מוגדר — ReferenceError בטעינת הצעה מהיסטוריה; הוגדר ב-app.js. **מובייל:** כפתורי מועדפים/תצוגה מהירה גלויים במגע (`@media (hover:none)` ב-style.css; בתצוגת רשימה נשארו מוסתרים בכוונה); כפתור הנגישות והתפריט הורמו מעל בר ההזמנה (`body:has(.fab-container)` ב-accessibility.css); נעילת גלילת רקע בכל המודאלים (syncModalScrollLock ב-app.js). **שונות:** אייקון הוואטסאפ הצף הוא SVG מוטמע (היה נטען מ-wikimedia); דף הסוכן — כל האמוג'ים הוחלפו ב-SVG והטורקיז ‎#00897b בירוק המותג; determineImageBaseUrl מחזיר '/' קבוע (בוטלו 5 בדיקות רשת בכל טעינה). תמונת אבי004 קיימת ונטענת — לא נדרש תיקון. גרסאות: app.js?v=18, style.css?v=13, accessibility.css?v=4, sw cache-v4. אומת בדפדפן: דף בית, קטלוג (רשת+רשימה), דף קטגוריה, דף סוכן (PIN ישן נדחה/חדש עובד, מודאל), מובייל 375px.

10. **SEO — סבב אוטומטי (13.7.26, הוחל על הריפו החי aroan):** סכמת `LocalBusiness` ל-`/contact/` + `AboutPage/Organization` ל-`/about/` (כולל Open Graph שלא היה); תמונת OG ממותגת `images/og-image.png` (1200×630) והחלפת כל הפניות og:image מ-logo.png אליה (19 עמודים); **דף עיר חדש `/or-yehuda/`** (schema WebPage+Breadcrumb+LocalBusiness) מקושר מהפוטר, מדף הבית, מ-llms.txt ומ-sitemap; `sitemap.xml` — lastmod ל-2026-07-13 + or-yehuda (19 URLs). **בנוסף תוקן פער:** ה-`<title>` של דף הבית היה עדיין הישן ("הבית של מוצרי הניקיון") ושודרג ל"ספק סיטונאי לחומרי ניקיון, נייר וחד פעמי לעסקים | אהרוני". כל ה-JSON-LD אומת. **לא נדחף ל-git — צריך commit+push.** דורש רועי: אימות אינדוקס + הגשת sitemap ב-Search Console, הקמת GBP, sameAs.

11. **סבב חיזוק "חד פעמי" ו"ציוד משרדי" (13.7.26, על בסיס נתוני GSC):** ניתוח GSC (אפר–יול 26) הראה שכמעט כל ההופעות הן על "שיווק חומרי ניקוי"; לדף disposables היו 0 הופעות ול-office-supplies ‏2 הופעות במקום 32. מחקר מתחרים: גלניר/אור פעמי/סילקו (חד פעמי), אופיר ספקים/קותי/מיטב (ציוד משרדי) — כולם עם כותרות exact-match ו-800–1,500 מילים בדף. **בוצע (בלי שינוי נראות):** כרטיסיות הקטגוריות בדף הבית הוסבו מ-`/catalog/?category=...` לדפים הסטטיים (cleaning/paper/disposables); כותרות ותיאורי מטא חדשים ל-disposables ("חד פעמי בסיטונאות לעסקים | כלים חד פעמיים ואריזות"), office-supplies ("ציוד משרדי לעסקים | קטלוג ציוד משרדי במחירים סיטונאיים"), catalog ודף הבית (כולל og:title ישן שהוחלף); keywords בסכמות; צ'יפ קישור הדדי בין disposables↔office-supplies. אומת בדפדפן. **ממתין:** תוספות טקסט בדפי הקטגוריה (רועי דחה בינתיים — 14.7), אינדוקס ידני של שני הדפים ב-Search Console (רועי ביצע 14.7; ה-404 בדוח GSC הם /card ו-/digitalcard הישנים — לא לטפל).

12. **דפי נחיתה עירוניים (14.7.26):** רועי ביקש עוד דפי נחיתה במקום תוספות טקסט. נוצרו 6 דפי עיר מתבנית or-yehuda (סקריפט: scratchpad, חד-פעמי): `/yehud/`, `/kiryat-ono/`, `/ramat-gan/`, `/bnei-brak/`, `/petah-tikva/`, `/tel-aviv/` — כולם מובילים עם "חד פעמי" ו"ציוד משרדי" בכותרת וב-H1, תוכן ייחודי פר עיר (יהוד=קרבה, ק.אונו=מוסדות חינוך, ר"ג=מגדלי משרדים, ב"ב=מוסדות ואירועים, פ"ת=קריית אריה/סגולה, ת"א=אריזות למשלוחים), סכמת WebPage+Breadcrumb+LocalBusiness עם areaServed עירוני. קישורים: sitemap (25 URLs, lastmod 14.7), llms.txt, שמות הערים בפסקת דף הבית וב-or-yehuda הפכו לקישורים, צ'יפים הדדיים בין דפי הערים. הפוטר לא שונה בכוונה (לא להאריך את העמודה). אומת בדפדפן + JSON-LD. דורש רועי: בקשות אינדוקס ידניות לדפים החדשים ב-Search Console.



13. **תיקון הצעות חיפוש בדף הסוכן (15.7.26):** ההצעות בחיפוש הוצגו ענקיות וחסמו את המסך — עיצוב `.search-suggestions` היה רק ב-site.css שדף הסוכן לא טוען. הועתק ל-style.css (עם משתני style.css, כולל מצב כהה) ליד תיקון "האייקונים הענקיים" הקיים. גרסאות: style.css?v=14 (12 דפים), sw cache-v5. אומת בדפדפן בסוכן ובקטלוג. commit `3070c02` — ממתין ל-push.

14. **חיבור Google Analytics 4 (15.7.26):** רועי חיבר את חשבון הגוגל ל-Claude דרך Composio (קריאה בלבד — דוחות כן, ניהול לא). נוצר נכס GA4 **aroam.co.il** (רועי יצר ידנית לפי הוראות; property id ‏545698526, חשבון accounts/379713876, מזהה מדידה **G-W1MRCCC3FS**). הוטמע **gtag.js ישירות** בכל 29 הדפים הציבוריים (אחרי קטע ה-GTM; לא דרך GTM כי אין גישה לממשק שלו) — כולל דפי קטלוג, ערים ודף הסוכן. ארבעת אירועי ההמרה מדווחים עכשיו גם ל-GA4 דרך `gtag('event')` בנוסף ל-dataLayer: ‏phone_click/whatsapp_click ו-form_submit ב-components.js, ‏order_sent (whatsapp/email) ב-app.js — עטופים ב-`typeof gtag === 'function'`. גרסאות: app.js?v=19, sw cache-v6. אומת מקצה לקצה: דוח Realtime דרך Composio הראה page_view + phone_click נקלטים בנכס. **דורש רועי:** לתקן את שם הנכס בממשק GA (נרשם בטעות "aroma.co.il") ולסמן את 4 האירועים כ-Key Events (ניהול ← אירועים).

15. **קטלוג טיפוח והיגיינה נסתר + מערכת עריכה (16.7.26):** נקודת שחזור: `3c22c56` (מסמך עיצוב: docs/superpowers/specs). **עמוד נסתר `/care/`** (במקור נוצר כ-/p-3167edfa/ ושונה באותו יום לבקשת רועי — קטלוג טיפוח והיגיינה) — קטלוג מלא עם מחירים (עגלה, פרטי לקוח, וואטסאפ/מייל), מבוסס על catalog/index.html עם `dataSource: '/data/pricelist.csv'`. נגיש רק בקישור ישיר: `noindex,nofollow`, לא ב-sitemap/llms.txt, **בכוונה לא ב-robots.txt** (היה חושף את הכתובת). **נתונים:** `data/pricelist-master.csv` (עמודות cost ו-sale_price — מקומי בלבד, ב-.gitignore חדש) ← ממנו נגזר `data/pricelist.csv` הציבורי בכל שמירה: בלי עלות; כשיש מבצע — price=מחיר המבצע ו-original_price=המחיר הרגיל. **עורך `/admin/pricelist.html`+`pricelist.js`** (מקומי בלבד, כמו admin — חסימת hostname): שלושה שדות מחיר עלות/מכירה/אחוז רווח — הזנת כל שניים מחשבת את השלישי (רווח מהעלות: (מכירה−עלות)/עלות); **שדה מחיר מבצע** אופציונלי (ולידציה: חייב להיות נמוך מהמכירה, ריקון = ביטול מבצע); העלאת CSV עם זיהוי כותרות עברית/אנגלית (כולל "מחיר מבצע"), מיזוג לפי מזהה/שם ותצוגה מקדימה לפני החלה; מזהים אוטומטיים PLxxx. **server.py:** endpoint `/api/pricelist` כותב את שני הקבצים. **app.js:** שינויים — `CATALOG_CONFIG.dataSource` עוקף את נתיב ה-CSV, ותמיכת מבצע: כשב-CSV יש original_price גבוה מהמחיר מוצג מחיר קודם מחוק + תג "מבצע" (בכרטיס ובתצוגה המהירה; העגלה ממילא מחשבת לפי price). ב-products.csv אין את העמודה — אפס השפעה על שאר הדפים. עיצוב `.price-old`/`.sale-badge` ב-style.css. גרסאות: app.js?v=21, style.css?v=15, sw cache-v8. אומת בדפדפן: עמוד נסתר (מבצע בכרטיס+תצוגה מהירה, עגלה לפי מחיר מבצע), עורך (חישוב תלת-כיווני, מבצע, ייבוא CSV, שמירה לשני הקבצים), קטלוג רגיל ודף סוכן לא נפגעו, מובייל 375px. **התאמת תמונות אוטומטית (16.7):** endpoint ‏GET `/api/list-images` ב-server.py + כפתור בעורך שממלא תמונות למוצרים בלי תמונה לפי שם קובץ = שם מוצר/מק"ט (נירמול גרשיים/מקפים); תיקייה ייעודית `images/מחירון/`. המוצרים בקטלוג הטיפוח שונים מהקטלוג הראשי ברובם — התמונות מגיעות מקבצים שרועי שם בתיקייה. **משיכה מגוגל שיטס (17.7):** כפתור בעורך + endpoint ‏GET `/api/fetch-sheet?id=` ב-server.py (דרך curl — ל-python3 של מק אין תעודות SSL); הגיליון של רועי (ID ‏1Qklkzyyg0REk0MubDNRuI0Idju523et807rxi4MSwac, שמור גם ב-localStorage, החלפה ב-Shift+לחיצה) חייב להישאר משותף-בקישור; במיפוי הכותרות "מחיר" לבד = **עלות** (כך בגיליון של רועי), מכירה מגיעה מ"מחיר מכירה"/"מכירה"; 52 המוצרים יובאו 16.7; מחירי המכירה נמשכו מהגיליון 17.7 (51/52 — חסר: ג'ילט קצף גילוח; חסרה תמונה: תחבושות אולוויז מידה 4). **באנר מבצעים + מבצע קבוצתי (17.7):** בטבלת העורך עמודת סימון + כפתור "מבצע לנבחרים" — הנחה באחוזים ממחיר המכירה (עיגול לעשירית ₪) או ביטול מבצע לכל המסומנים. **הבאנר אוטומטי (17.7 אחה"צ):** נבנה ב-care/index.html מהמוצרים שבמבצע (תמונה, מחיר מחוק, מחיר מבצע), מתחלף כל 4 שניות, קליק מעביר לקטגוריית מבצעים; טקסט ידני מהעורך (data/pricelist-banner.json, ‏POST `/api/banner`) מתווסף כשקופית ראשונה אם הופעל. **קטגוריית "מבצעים" ב-app.js** — כפתור ראשון (עיצוב `.cat-btn-sale` אדום ב-style.css), מופיע רק כשיש original_price>price, כלומר רק בקטלוג הטיפוח; activeCategory='sales'. רועי הפעיל מבצע 10% על 4 מוצרי ARGANIA (17.7). **סבב B2C מורחב (18.7):** תוקן `allowShare:true` ב-care (כפתורי השליחה היו מוסתרים!); סה"כ מחיר על הכפתור הצף (fab-total, נבנה דינמית ב-updateSummary); הודעת ההזמנה כוללת מחירים+סה"כ כשמוצגים מחירים; **עמודת brand** במחירון (master+public, מולאה אוטומטית ל-52; שדה בעורך+ייבוא; סינון מותגים בעמוד עובד דרך app.js הקיים); הבאנר sticky; **מבצעים כלליים** — `data/pricelist-promo.json` (החליף את pricelist-banner.json), ‏POST `/api/promo`, עורך: כרטיס עם באנר/הנחת סל (רף ₪ + %)/הנחות קטגוריה: הנחת קטגוריה מוחלת ב-app.js (applyPromotions, מבצע פרטני גובר, עיגול ל-10 אג'), הנחת סל מוצגת בעגלה+הודעה+fab (cartDiscountFor), שניהם מקבלים שקופיות בבאנר; הסכמה משקפת הנחות קטגוריה. תוקן topbar נחתך בעורכים (flex-wrap ב-admin.css). **מובייל (19.7):** ב-care בלבד (CSS inline בעמוד, בלי לגעת ב-app.js/בקטלוג הראשי) — טבלת ההזמנה במובייל ≤768px הופכת לכרטיסים אנכיים בלי גלילה אופקית (grid לפי nth-child: תמונה/שם/כמות/סה"כ; מק"ט, קטגוריה, יחידה ומחיר-יחידה מוסתרים), קלטים 16px נגד זום iOS, כפתורי שליחה ברוחב מלא. גרסאות: app.js?v=24, style.css?v=16, sw cache-v11. אומת בדפדפן כולל עגלת 404₪ עם הנחה 40.40. ממתין ל-push של רועי.

16. **סבב ביצועים ונגישות לפי PageSpeed (19.7.26 ערב):** דוח PSI לקטלוג במובייל: ביצועים 70 (FCP ‏3.5s, LCP ‏5.4s), נגישות 90, SEO ‏92. בוצע: **פונטים לא-חוסמים** בכל 27 הדפים (preload + ‏media="print" onload + noscript — ‏display=swap כבר היה); **preload ל-products.csv** ב-11 דפי הקטלוג (סקריפט inline שמחשב את אותו URL שעתי של app.js — אומת שאין הורדה כפולה); **נגישות:** aria-label לחיפוש (קטלוג/סוכן/care/catalog-shell) ולכל שדות הכמות ב-app.js; **ניגודיות:** קישור ניווט פעיל הוסב מפסטל ‎#8FD6A3 (יחס 1.71!) לירוק כהה ‎--secondary-color, ‏--text-secondary במחירון ‎#757575→#6B6B6B. **בכוונה לא שונה:** לבן על ירוק המותג ‎#639C7D בכפתורים (יחס 3.19 — נכשל AA לטקסט קטן; שינוי = החלטת מיתוג של רועי). לא בשליטה: cache קצר של GitHub Pages (‏287KB), JS של GTM/gtag. שגיאת robots.txt בדוח = cache ישן (תוקן באותו ערב ב-e34da17). גרסאות: style.css?v=17, app.js?v=25, sw cache-v12. אומת מקומית: קטלוג + דף קטגוריה, preload מנוצל, אפס שגיאות קונסול. אחרי push — להריץ PSI מחדש.

17. **שיפורי המרה + הפרדת B2C (20.7.26):** הוצע לרועי סבב שיפורים; אושרו 1-4 + הסרת ניווט. **בוצע (משימות 1-3, ‏app.js/css/care בלבד — בלי לגעת בקטלוג הראשי/סוכן):** (1) **עגלה נשמרת** — `CART_KEY = aroam_cart_<path>`, ‏updateSummary שומר ל-localStorage, ‏init משחזר (פרמטר cart ב-URL עדיין גובר); חל על כל הקטלוגים אבל בטוח. (2) **נאדג' "עוד X ₪/פריטים למבצע"** — `updatePromoNudge` מציג בבר תחתון (#promo-nudge-bar) ובראש המודאל (#promo-nudge-modal) כמה חסר לרף הנחת הסל, או "מזל טוב קיבלת הנחה"; רק כש-CART_PROMO קיים. (3) **כפתור "הוסף לסל"** — `CONFIG.showAddButton` (רק ב-care), כשכמות=0 מוצג `.add-to-cart-btn` בולט, בלחיצה מתחלף לבקר +/- (syncAddState). **הסרת ניווט לאתר המלא מ-care (לבקשת רועי):** `<site-header>`/`<site-footer>` הוחלפו בכותרת+פוטר עצמאיים inline ב-care/index.html (לוגו→/care/, טלפון, פרטי קשר + נגישות/פרטיות בלבד; אפס קישורים ל-catalog/about/ערים); components.js עדיין נטען (וואטסאפ צף). אומת: 0 קישורים דולפים לאתר המלא, עגלה שורדת רענון, נאדג' 330.10₪, כפתור הוסף מתחלף. גרסאות: app.js?v=25, style.css?v=17 (כבר עודכנו בסבב 16). **משימה 4 — פיד Google Merchant Center (20.7):** `update_merchant_feed()` ב-server.py כותב `data/merchant-feed.xml` (RSS 2.0 עם `g:` namespace) בכל שמירה, לצד הסכמה; קורא מ-pricelist.csv, מדלג על מוצרים בלי מחיר/תמונה (51/52), מחיר אפקטיבי דרך `_effective_price` (מבצע פרטני/הנחת קטגוריה, עיגול 10 אג'), מבצע פרטני→g:price רגיל+g:sale_price, ‏g:brand, ‏g:identifier_exists=no (אין GTIN), ‏g:link עם ?product=id. שותפו helpers `_load_category_discounts`/`_effective_price` עם update_care_schema (רפקטור). **דורש רועי:** לפתוח חשבון Google Merchant Center (חינם) ולהזין את הפיד: `https://aroam.co.il/data/merchant-feed.xml`. **משימה 5 — דפי קטגוריה סטטיים ל-SEO (20.7):** מטרת רועי — הקטלוג יופיע גבוה בגוגל בחיפוש מוצר. הבעיה: /care/ טוען מוצרים ב-JS, ה-HTML הראשוני ריק. הפתרון: `generate_care_category_pages()`+`_render_care_category_html()`+`CARE_CATEGORY_PAGES` ב-server.py יוצרים 6 דפי SEO סטטיים תחת `care/<slug>/` (שיער→hair, כביסה→laundry, היגיינת פה→oral-care, נשים→feminine, גבר→men, כללי→general) בכל שמירה. כל דף: כותרת/description/H1 ממוקדי-קטגוריה, רשימת מוצרים קריאה לגוגל (שם+מותג+תיאור+מחיר/מבצע), סכמת CollectionPage+ItemList+BreadcrumbList, כותרת+פוטר עצמאיים (בלי site-header), CTA ל-`/care/?category=<שם>` (app.js תומך ב-deep link), קישורים הדדיים. דף תוכן קליל בלי app.js — ההזמנה נשארת בקטלוג האינטראקטיבי. sitemap+llms.txt עודכנו (6 כתובות), בלוק `.care-cat-links` ב-care/index.html מקשר אליהם. אומת: 6 דפים, אפס דליפות ל-B2B, CTA מסנן נכון, מובייל. בנוסף `bake_care_products()` ב-server.py מזריק את 52 המוצרים כטקסט קריא (h2+מותג+מחיר+תמונה) ל-#products בעמוד /care/ הראשי בין CARE_PRODUCTS_START/END — כי ה-HTML הראשוני היה ריק; app.js מנקה ומרנדר מחדש בטעינה (אומת: אפס כפילות, עגלה תקינה). sw cache-v13. **דורש רועי:** push + Search Console (הגשת sitemap + בקשת אינדוקס ל-6 הכתובות + /care/). **הבהרה לרועי:** הבסיס הטכני ל-SEO הושלם, אבל דירוג בפועל בגוגל תלוי באינדוקס (שבועות) ובפעולות ה-Search Console שלו — לא ניתן להשלמה מסביבת Claude. **ממתין:** משימות 6-8 ב-TODO. ממתין ל-push.

19. **יישור מונח מפתח בדף הבית ובדפי ערים (20.7.26):** מחקר מתחרים (גלניר, דוסתרי, ניתוב חוסן, זעפרני, סטאר — כולם "חומרי **ניקוי**") + נתון GSC היסטורי (הופעות על "חומרי ניקוי") הראו שהמונח המסחרי המוכח הוא "ניקוי" (ו') ולא "ניקיון". דף הבית היה לא-עקבי: description השתמש ב"ניקוי" אבל title+H1+og:title ב"ניקיון". תוקן: **דף הבית** title/og:title/H1 → "חומרי ניקוי"; **or-yehuda** title/og:title/H1/schema-name → "ניקוי"; **yehud** title/og:title/schema-name → "ניקוי". התיאורים והטקסט בגוף נשארו עם "ניקיון" בכוונה — כך העמודים תופסים את **שני** הווריאנטים, עם המונח החזק במיקומים החזקים (title/H1). שינוי טקסט/מטא בלבד. אומת בדפדפן (title/H1/og + JSON-LD תקין בדף הבית ו-or-yehuda). ממתין ל-push.

18. **סבב קידום אורגני — רשת קישורים פנימיים מקומית (20.7.26):** רועי הגדיר מטרה: שהאתר יופיע יותר בחיפושים. אושרה תוכנית מבוססת-נתונים (`~/.claude/plans/jiggly-honking-aho.md`): לבסס על Search Console, איזון מוצרים+מקומי, רק שיפור עמודים קיימים. **חובר Search Console דרך Composio — אבל רועי טרם אישר את קישור ה-OAuth, אז שלב הנתונים (striking-distance queries) ממתין.** בוצע בינתיים החלק הבטוח שלא תלוי ב-GSC (שלב 3 בתוכנית): **שורת "אזורי חלוקה ושירות"** נוספה לבלוק `.cat-seo` בכל 10 דפי הקטגוריה — צ'יפים בסגנון `.cat-links` הקיים (בלי CSS חדש) עם 7 קישורים לדפי הערים (or-yehuda, kiryat-ono, yehud, ramat-gan, bnei-brak, petah-tikva, tel-aviv). סה"כ 70 קישורים פנימיים חדשים category→city — דפי הערים כמעט לא קיבלו קישורים נכנסים קודם. שינוי HTML בלבד (אין שינוי JS/CSS → אין העלאת גרסאות). אומת מקומית (DOM+CSS מחושב: צ'יפים תקינים, JSON-LD CollectionPage תקין, 7 קישורים מרונדרים). **המשך (20.7 אחה"צ):** 4 דפי הנחיתה לפלחים (offices/restaurants/factories/education) היו **מבוי סתום** — 0 קישורים פנימיים לקטגוריות/ערים. נוסף בכל אחד בלוק "קטגוריות מבוקשות" + "אזורי חלוקה ושירות" לפני הפוטר (5 קטגוריות רלוונטיות פר-פלח + 7 ערים; צ'יפים inline-styled בסגנון האתר, בלי CSS משותף/גרסאות). אומת בדפדפן (13 צ'יפים, פילס תקין, JSON-LD תקין, לפני site-footer). **ממתין:** אישור GSC של רועי → שליפת שאילתות → חיזוק Title/meta/H1 ממוקד לעמודי מרחק-נגיעה (שלבים 1-2). Google Business Profile (ROI מקומי גבוה). ממתין ל-push.

- **להחליף את סיסמת ה-API בסקריפט היסטוריית ההצעות בענן** (Apps Script של AgentQuotes): הסיסמה Aroam2026 נחשפה בעבר בקבצים פומביים ובהיסטוריית git — לקבוע סיסמה חדשה בסקריפט, ובדף הסוכן להזין אותה כשנשאלים (נשמרת ל-session).
- ~~התקנת `tools/google-apps-script-orders.gs`~~ — **בוצע 31.7.26** (ראה סבב 28).
- סימון 4 אירועי ההמרה כ-Key Events בממשק GA4 (החיבור עצמו בוצע 15.7 דרך gtag ישיר — GTM כבר לא נדרש לזה).
- **מעבר על 176 תיאורי המוצר בעורך** (נכתבו אוטומטית בסבב 29 — כדאי לוודא שהניסוח מדויק לפני שגוגל מאנדקס). נוספה **עמודת תיאור עם עריכה ישירה בטבלה** כדי שאפשר יהיה לעבור עליהם ברצף בלי לפתוח חלון לכל מוצר.
- **מילוי הדרגתי של עמודת האריזה/קרטון** בעורך — מוצג רק כשמולא, וקריטי לקונה עסקי.
- הרצת סקריפט התמונות ובדיקת התוצאה.
- לוודא שכל השינויים נדחפו ל-GitHub (push) + שליחת sitemap ב-Search Console.
- **Google Business Profile — קיים ומקושר** (1.8.26). מזהים: kgmid `/g/11zjgtcsdn`, feature id `0x983c5defa0b5c6b:0x394e4d25682cac1e`, **CID `4129322731402210334`** → `https://www.google.com/maps?cid=4129322731402210334`. נוסף כ-`sameAs` לשלוש סכמות העסק (LocalBusiness ב-/contact/, WholesaleStore בדף הבית, Organization ב-/about/). **לא נוסף `aggregateRating`** (בפרופיל 5.0 מ-6 ביקורות) — סימון דירוג עצמי באתר שלך נוגד את הנחיות גוגל.
- **פערים בפרופיל שדורשים את רועי (1.8.26):** (א) **הטלפון בפרופיל הוא 03-6346236** בעוד ש-`telephone` בסכמות האתר הוא הנייד `+972526000158` — כדאי ליישר; (ב) **לא מוצגת כתובת בפרופיל** — אם העסק מוגדר כ-service-area זה תקין, אחרת כדאי להוסיף את היצירה 16 אור יהודה כדי לחזק את ההתאמה ל-NAP באתר; (ג) קטגוריה ראשית בפרופיל: "ספק מוצרי ניקיון".
- רעיונות שאושרו עקרונית אך לא בוצעו: מבצעי החודש, דף שאלות נפוצות (צריך תשובות מרועי), תמונת הירו אמיתית (צריך צילום). *("הוסף הכל לסל" ומידע אריזה — בוצעו בסבב 29.)*

20. **סבב שיפורי המרה + תשתית משפטית + כלים תפעוליים (21.7.26):** רועי אישר סבב שיפורים והמשך משימות. **המרה (care/app.js/css בלבד):** (1) עגלה נשמרת ב-localStorage לפי נתיב (CART_KEY), (2) נאדג' "עוד X ₪/פריטים למבצע" בבר התחתון+מודאל (updatePromoNudge), (3) כפתור "הוסף לסל" בולט שמתחלף לבקר כמות (CONFIG.showAddButton, רק ב-care). **הסרת ניווט לאתר המלא מ-care:** site-header/site-footer הוחלפו בכותרת+פוטר עצמאיים inline (לוגו→/care/, טלפון, פרטי קשר+נגישות/פרטיות/תקנון). **פיד Merchant (data/merchant-feed.xml):** update_merchant_feed() ב-server.py — RSS 2.0 עם g: namespace, 51 מוצרים, מבצע→price+sale_price, ‏g:brand, קישור עמוק ?product=id. רועי פתח חשבון Merchant. **6 דפי קטגוריה סטטיים ל-SEO** (care/<slug>/): generate_care_category_pages()+CARE_CATEGORY_PAGES ב-server.py — כותרת/H1/תיאור ממוקדים, מוצרים כטקסט קריא, סכמת CollectionPage+ItemList+Breadcrumb, CTA ל-/care/?category=X, בלי app.js. sitemap+llms.txt עודכנו. רועי הגיש אינדוקס ב-Search Console. **תיקון סרגל עורך:** admin.css — .main-content overflow-y:auto, .topbar sticky+flex-shrink:0 (כפתורים לא נחתכים כשנשברים לשורה). **תשתית משפטית (טיוטות לעו"ד):** terms/privacy עודכנו למכירת B2C — זכות ביטול/החזרה לפי חוק הגנת הצרכן (כולל חריג מוצרי היגיינה שנפתחו), מחירים+מע"מ, איסוף נתוני הזמנה. **4 מקומות [להשלמה]:** מספר עוסק, מדיניות החזרות מדויקת, אמצעי תשלום, דמי משלוח. **מתג פעיל/מוסתר (עמודת active במאסטר):** מוצר מוסתר (active=0) נשאר במאסטר אך לא נכתב ל-public → נעלם אוטומטית מקטלוג/סכמה/פיד/דפי קטגוריה; כפתור 👁️/🚫 בעורך. **דף QR להדפסה** (care/print/): פלייר A4 עם QR מוטמע (segno) ל-/care/. הכל אומת בדפדפן. ממתין ל-push.

21. **דפי מוצר סטטיים ל-/care/ (SEO, 22.7.26):** עד כה ל-/care/ היו רק דף ראשי + 6 דפי קטגוריה — אין דף פר-מוצר, כך שחיפושי מוצר ספציפי בגוגל ("מחיר שמפו ארגניה") לא נחתו על דף ייעודי. **חדש ב-server.py:** `generate_care_product_pages()` (נקרא בשני endpoints של השמירה, לצד שאר הגנרטורים) יוצר דף סטטי אינדקסבּילי לכל מוצר עם מחיר>0 תחת **`care/product/<slug-עברי>/index.html`** (63 דפים) — כותרת/description ממוקדים, `canonical` עצמי, `robots index,follow`, og:product, **סכמת `Product` מלאה** (Offer מחיר-אפקטיבי/מבצע, brand, sku, image, description) + `BreadcrumbList`, גוף קליל (בלי app.js): תמונה, מחיר/מבצע מחוק+תג, יחידה, תיאור, **CTA `/care/?category=<cat>&product=<id>`** (גולל+מדגיש את הכרטיס בקטלוג — app.js קיים), ובלוק "מוצרים נוספים בקטגוריה" (עד 6 קישורים). ה-slug נגזר בעברית מהשם דרך helper משותף `_care_product_slugs(rows)` (דטרמיניסטי, ייחודי) + `_slugify_he`. **כל הקישורים הפנימיים הופנו לדפי המוצר** (במקום `?product=id`): כרטיסי דפי הקטגוריה, המוצרים המוזרקים ב-`bake_care_products`, ו-`g:link` בפיד Merchant. **ניקוי אוטומטי:** `_cleanup_stale_product_pages` מוחק תיקיות מוצר שאין להן מוצר קיים (הוסתר/שונה שמו/נמחק). **sitemap.xml:** 63 כתובות מוצר מוזרקות בין `<!-- CARE_PRODUCTS_SITEMAP_START/END -->` (95 URLs סה"כ, אידמפוטנטי). אפס נגיעה ב-app.js/קטלוג ראשי/סוכן/B2B; אין העלאת גרסאות (הדפים לא טוענים app.js/css). אומת בדפדפן: דף מוצר (מבצע+CTA), deep-link גולל לכרטיס, מוצר בלי תמונה (placeholder "אין תמונה"), מובייל 375px, JSON-LD Product+Breadcrumb תקין, ניקוי דף מיושן, sitemap XML תקין. **דורש רועי:** push + Search Console (הגשת sitemap מחדש + בקשת אינדוקס לדפי המוצר). **הערה:** דורש שמירה מהעורך (או הרצת הגנרטורים) כדי לרענן את הדפים כשהמחירון משתנה — כבר מחווט לשמירה.

22. **חיפוש אתר + FAQ + אבחון אינדוקס (22.7.26):** רועי דיווח שחיפוש מוצר בשמו לא מוצא אותו ורוצה להוביל בתחום. **(א) תוקן באג חיפוש** ב-app.js: החיפוש התאים רק substring רציף, כך ש"אולוויז מידה 4" החזיר 0 (המוצר "תחבושות אולוויז אולטרה לילה סקיור נייט מידה 4"). עכשיו כל מילה נבדקת בנפרד (בכל סדר) מול שם+מותג, מק"ט מלא כ-fallback, ונירמול מרכאות/גרשיים (מ"ל=מ״ל). חל על כל הקטלוגים. גרסאות: app.js?v=26, sw cache-v14. אומת בדפדפן. **(ב) FAQ + סכמת FAQPage** ל-6 דפי הקטגוריה של /care/ (`_care_faq()` + הזרקה ב-`_render_care_category_html`): ש"ות מדויקות (הזמנה, אזור חלוקה, מע"מ, החזרות לפי חוק הגנת הצרכן, מבצעים), שאלה ראשונה ממוקדת-קטגוריה — טקסט גלוי + JSON-LD. **(ג) אבחון אינדוקס מבוסס-נתונים:** WebSearch ל-`site:aroam.co.il` החזיר אפס (מטעה — US-only), אבל **GA4 (30 יום) הראה שהאתר כן מאונדקס**: 56 Direct, **10 Organic**, 3 AI Assistant. דפי הנחיתה האורגניים: דף הבית, `/catalog/disposables`, `/catalog/office-supplies` (בדיוק הדפים עם כותרות exact-match+תוכן מסבב 11 — מוכיח שהתבנית עובדת), `/care/`=1 בלבד. **מסקנה:** הבעיה היא דירוג נמוך+סמכות דומיין נמוכה+דפים חדשים עדיין לא live, לא היעדר אינדוקס. **דורש רועי (הלֶבֶר האמיתי):** push; Search Console (הגשת sitemap + בקשת אינדוקס ל-/care/ ולדפי המוצר); Google Business Profile; קישורים נכנסים ממדריכים עסקיים (dunsguide/zap/b144/easy) — הסיבה שהמתחרים מדורגים. ממתין ל-push.

23. **העברת פיצ'רים מ-care לקטלוג הרגיל (23.7.26):** רועי ביקש להעביר שני פיצ'רים מקטלוג ההיגיינה לקטלוג הרגיל. **(א) כפתור "הוסף לסל"** — כל הלוגיקה כבר ב-app.js (`CONFIG.showAddButton`: כפתור בולט כשכמות=0, מתחלף לבקר +/− אחרי הוספה, `syncAddState`); הופעל ע"י הוספת `showAddButton: true` לקונפיג של `catalog/index.html` + 10 דפי הקטגוריה (לא בסוכן). ה-CSS `.add-to-cart-btn` כבר היה ב-style.css. **(ב) הצגת הזמנה כרטיסים במובייל** — מומש **נקי ומשותף** במקום ה-nth-child האינליין של care: נוספו classes סמנטיים לתאי טבלת ההזמנה ב-app.js (`oc-img/oc-sku/oc-name/oc-cat/oc-qty/oc-unit/oc-price/oc-total` — תוספת בלבד, אפס שינוי התנהגות), וכלל `@media(max-width:768px)` אחד ב-style.css שהופך את הטבלה לכרטיסים (grid: תמונה+שם+כמות, ומק"ט/קטגוריה/יחידה/מחיר-יחידה מוסתרים). עובד גם עם מחירים (care/סוכן — מציג `oc-total` סה"כ-לשורה) וגם בלי (קטלוג רגיל — בלי סה"כ, כי אין מחירים). **הבלוק האינליין של care הוסר** (care עכשיו על הכלל המשותף). **הבהרה חשובה לרועי:** סה"כ הסל (`#summary-total` + `#fab-total`) הוא אלמנט נפרד מהטבלה ולא הושפע — הלקוח ב-care רואה סה"כ-לשורה + סה"כ-סל כרגיל (אומת בדפדפן). הכלל המשותף גם משפר את תצוגת ההזמנה במובייל בדף הסוכן (תופעת לוואי חיובית; כפתור ההוספה לא נוסף שם). גרסאות: app.js?v=27, style.css?v=18, sw cache-v15. אומת בדפדפן במובייל 375px: קטלוג רגיל (כפתור מתחלף, כרטיסי הזמנה), דף קטגוריה (39 כפתורים), care (סה"כ-לשורה 10.90/19.90 + סה"כ-סל 30.80, אפס שגיאות קונסול). ממתין ל-push.

24. **ביצוע משימות ה-SEO של B2B מהמסמך (23.7.26):** רועי ביקש לבצע את "שאר המשימות" ממסמך aroam-seo-tasks.html **בלי FAQ**. בוצע בקוד (הכל committed, ממתין ל-push): **(משימה 6) הרחבת תוכן דפי הקטגוריה** — בלוק `.cat-seo` בכל אחד מ-cleaning/disposables/paper/office-supplies הורחב מ-~120 מ' ל-~260-360 מ' עם h3-סעיפים אמיתיים (קטגוריות מוצר מפורטות, מותגים, אריזות מוסדיות, מגזרי לקוח) + קישורים פנימיים; נוסף CSS ל-`.cat-seo h3/strong/a` inline בכל דף. **(משימה 8) חיזוק דפי מגזר** — restaurants: סעיף תוכן עם exact-match "חד פעמי למסעדות" (היה מיקום 17); offices: סעיף "ספק אחד" עם קישורים לכל קטגוריות הרכש. **(משימה 11) דף אזורי חדש `/gush-dan/`** — נחיתה אזורית (WebPage+Breadcrumb+LocalBusiness, areaServed כל גוש דן) המשמשת גם hub פנימי המקשר לכל 7 דפי הערים; קישור פוטר סייטווייד (components.js, לא דולף ל-care כי ל-care פוטר עצמאי) + sitemap + llms. **(משימה 9) מרכז מדריכים `/guides/`** — hub (CollectionPage) + **5 מדריכי קנייה** (Article+Breadcrumb, ~200-270 מ' כל אחד): בחירת ספק חומרי ניקוי, חד פעמי למסעדות, כמה נייר צריך משרד, ניקוי מטבח מסחרי, צ'קליסט לעסק חדש — תוכן top-of-funnel עם קישורים פנימיים חזקים; קישור פוטר סייטווייד. **(משימה 7) קניבליזציה בדף הבית — נבדק, לא נדרש שינוי:** דף הבית כבר ממוקד "ספק סיטונאי" בכותרת/H1 ומעביר מונחי מוצר לדפי הקטגוריה בעוגנים מדויקים (מסבבים 11/19). sitemap: 99 URLs. אף שינוי ב-app.js/style.css/גרסאות (רק components.js תחת sw cache-v15 הקיים). כל דף אומת בדפדפן (מילים, JSON-LD, קישורים, אפס שגיאות קונסול). **לא בוצע לפי בקשת רועי:** FAQ לדפי B2B ולדפי מוצר. **דורש רועי:** push; Search Console (הגשת sitemap מחדש + בקשת אינדוקס ל-/gush-dan/ ול-/guides/); משימות ידניות (NAP, אינדקסים, קישורים נכנסים) שאינן קוד.

25. **מבצע חבילה (1+1 / 2+1) + תיחום מבצעים בתאריך (23.7.26):** רועי ביקש אופציה בעורך למבצעי 1+1/2+1 ולתיחום מבצע בתאריך, "שיהיה רשום בקטן וגם בבאנר". החלטות שאושרו: **פר מוצר** (לא קטגוריה/גלובלי), **תווית + חישוב אמיתי בעגלה** (לא תווית בלבד — כדי שלא יוצג "1+1" והלקוח יחויב על שתיים), ו**חלון גלובלי + דריסה פר מוצר**. מסמך עיצוב: `docs/superpowers/specs/2026-07-23-bundle-promo-design.md`. **נתונים:** 3 עמודות חדשות בשני קובצי המחירון — `bundle` ("X+Y"), `promo_start`, `promo_end` (חלים על כל המבצעים של המוצר, כולל מחיר המבצע) + `window: {starts, ends}` ב-pricelist-promo.json. **server.py:** helpers `_clean_bundle`/`_clean_date`/`_promo_active`/`_product_promo_active`/`_bundle_label`/`_promo_dates_for`/`_price_is_discounted`/`_sale_effective_date`; `_effective_price` חוזר למחיר הרגיל כשמבצע פג; `_load_category_discounts` מחזיר {} כשהחלון פג (כך הנחות קטגוריה כבויות בכל הגנרטורים). **app.js:** `chargeableQty(product, qty)` (כל buy+free → free חינם) מוחל ב-4 מקומות (updateSummary, שורת טבלת ההזמנה, הודעת ההזמנה, getCartItemsData); `promoActive`/`productPromoActive` בודקים תאריך בזמן אמת בדפדפן; מבצע פרטני שפג מוחזר למחיר הרגיל; תווית `.bundle-badge` + `.promo-validity` ("בתוקף עד DD.MM.YYYY") בכרטיס ובתצוגה מהירה; קטגוריית "מבצעים" כוללת מוצרי חבילה. **באנר care:** שקופית לכל מוצר חבילה + תג `.promo-bundle` ו-"בתוקף עד" קטן. **עורך:** שדה "מבצע חבילה" + תאריכי תוקף בטופס המוצר, "חבילה לנבחרים"/"בטל חבילה" במודאל הקבוצתי, שדות חלון גלובלי בכרטיס המבצעים, תגים בטבלה, ומיפוי כותרות לייבוא CSV. **חשיפה חיצונית:** `g:sale_price_effective_date` בפיד ו-`priceValidUntil` בסכמות — **גם עבור הנחות קטגוריה** (תוקן: קודם נבדק רק `original_price`, וההנחה מהקטגוריה נשארה בלי תוקף → אי-התאמה אפשרית ב-Merchant). **מגבלה מתועדת:** /care/ מתקן את עצמו בדפדפן, אבל דפים סטטיים/סכמה/פיד נבנים בזמן שמירה — אחרי סיום מבצע כדאי שמירה אחת בעורך. גרסאות: app.js?v=28, style.css?v=19, sw cache-v16. אומת מקצה לקצה בדפדפן: 2+1 עם 3 יח'=70.20, 1+1 עם 2 יח'=35.10, 3+1 עם 4 יח'=105.30, "N חינם" בשורה, הודעת וואטסאפ, מבצע שפג לא מוצג, דריסה פר-מוצר, באנר, עורך (round-trip + שמירה לקבצים), קטלוג B2B לא נפגע (0 תוויות), מובייל 375px, אפס שגיאות קונסול. נתוני הבדיקה נוקו — הפיצ'ר מוכן בלי מבצעים מוגדרים.

26. **מייל הזמנות + אוטומציות (23.7.26):** רועי ביקש שפרטי הלקוח מהטופס יגיעו גם למייל, וביקש הצעות לאוטומציות. **(א) מייל/גיליון ההזמנות:** התברר ש-app.js **כבר שולח** את `customer` בפיילוד (אומת בדפדפן) — הפער הוא שאצל רועי מותקנת גרסה **ישנה** של Apps Script שמתעלמת ממנו. הפתרון הוא התקנת `tools/google-apps-script-orders.gs` (ממתין מ-13.7). בנוסף שופר הסקריפט: שורות הפריטים כוללות עכשיו **סכום לשורה** ו-**"מבצע 2+1 — 1 חינם"** (כדי שרועי יידע כמה יחידות לתת באספקה), נוספה עמודת **"סה"כ לתשלום"** בגיליון ושורת סה"כ במייל; `getCartItemsData` ב-app.js שולח `bundle` ו-`free` בכל פריט. גרסאות: app.js?v=29, sw cache-v17. **(ב) אוטומציות (נבנו):** `tools/refresh_site.py` — מריץ את כל 5 הגנרטורים מכל תיקייה (משותף); `.github/workflows/daily-refresh.yml` — **ה-Action הראשון בפרויקט**, רץ יומית ב-03:00 UTC (06:00 בישראל) + workflow_dispatch, מריץ גנרטורים, מוודא תקינות XML של הפיד/sitemap ו-JSON-LD, ועושה commit **רק אם משהו השתנה** (permissions: contents write). זה פותר את המגבלה מסבב 25 — מבצע שנגמר/מתחיל משתקף באתר לבד בלי שמירה ידנית; **`פרסם לאתר.command`** — לחיצה כפולה: רענון → הצגת git status → commit עם תאריך → push, עם הודעות בעברית וטיפול בשגיאות (אומת: מסלול "אין שינויים", מסלול עם שינויים, וכשל push מציג הסבר ידידותי). **דורש רועי:** push (ה-Action יתחיל לרוץ אוטומטית אחריו). *(התקנת ה-Apps Script — בוצעה 31.7.26, ראה סבב 28.)*

27. **תאריך התוקף ליד כל מבצע + תאריכים נפרדים לכל סוג מבצע (31.7.26):** רועי ביקש "שיראו את תאריך המבצע ליד המבצע" ו"אופציה לשים תאריכים שונים למבצעים שונים". מסמך עיצוב: `docs/superpowers/specs/2026-07-31-per-promo-dates-design.md`. **שני פערים אמיתיים שאותרו בקוד:** (1) `appendBundleInfo` יצא מוקדם כשאין `bundle`, ולכן **מוצר עם מחיר מבצע רגיל או הנחת קטגוריה לא הציג תאריך כלל** — רק מבצעי חבילה. (2) `banner`/`cart_discount`/`category_discounts` חלקו `window` גלובלי יחיד. **הפתרון:** (א) הפרדת אחריות — `appendBundleInfo` = תג בלבד, ו-**`appendPromoValidity`** חדש שמוסיף `עד DD.MM` בטקסט קטן **inline צמוד לתג** לכל מוצר עם מבצע פעיל כלשהו (`hasActivePromo`) ותאריך סיום; `.promo-validity` שונה מ-block ל-inline. (ב) לכל מבצע `starts`/`ends` אופציונליים, עם **אותו כלל הכרעה שכבר קיים למוצרים** — תאריכי המבצע גוברים, אחרת החלון הגלובלי (`scopedActive` ב-app.js, `_scoped_active` ב-server.py). `_load_category_discounts` מחזיר עכשיו `{קטגוריה: {percent, ends}}` ומסנן כללים שפגו; `promoEndFor`/`_promo_dates_for` מורחבים לסדר: תאריכי המוצר → תאריך כלל הקטגוריה שחל → החלון. `priceValidUntil` ו-`g:sale_price_effective_date` נגזרים מאותו תאריך אפקטיבי, כך שהפיד תמיד תואם למוצג. **עורך:** שדה "עד תאריך" לבאנר, להנחת הסל ולכל הנחת קטגוריה (הצ'יפ מציג `שיער — 8% · עד 01.09.2026`). גרסאות: app.js?v=30, style.css?v=20, sw cache-v18. **אומת בדפדפן בתרחיש מלא:** מוצר עם מחיר מבצע בלבד (בלי חבילה) הציג `עד 20.08` — הפער העיקרי; שיער 8% עם `עד 01.09`; כביסה 12% עם תאריך שפג **לא הוחלה כלל**; הנחת סל עם תאריך משלה נותרה פעילה; round-trip מלא בעורך (banner 05.10, cart 31.08, קטגוריות נפרדות) נשמר לקובץ; `priceValidUntil` תאם (2026-09-01 / 2026-08-20); קטלוג B2B — 0 תוויות; אפס שגיאות קונסול. נתוני הבדיקה נוקו.

28. **התקנת סקריפט ההזמנות בפועל + שיפור הפיד (31.7.26):** רועי שאל איך מתקינים את הסקריפט ואמר שאינו יודע איזה פרויקט זה — ואז ביקש שאאתר בעצמי דרך Chrome. **(א) זיהוי:** בחשבון היו 10 פרויקטים, רובם "ללא שם". השיטה שעבדה: פתיחת כתובת ה-webhook מ-app.js בדפדפן (GET → נכשל ב-`doGet` כצפוי), ואז ההפעלה הופיעה בראש "ההפעלות שלי" עם קישור לפרויקט הנכון — "aroma order webhook". **אומת שהקוד הישן באמת מתעלם מ-`data.customer`** (קרא רק orderId/items/totalPrice/totalItems ובנה CSV מצורף) — האבחון מסבב 26 אושש. **(ב) התקנה:** נוצר גיליון "הזמנות אהרוני", הקוד הוחלף דרך Monaco (`monaco.editor.getModels()[0].setValue`) כי הקלדה בעורך קוד משבשת הזחה והגישה ללוח נדחתה, הורץ `testOrder` (**נכשל בהתחלה כי הרצתי בטעות `doPost` — הגיליון הריק חשף את זה**; אחרי בחירה נכונה: שורה מלאה עם כל פרטי הלקוח + "סה"כ לתשלום 105.3" + סטטוס נפתח), ופורסמה **גרסה 2** על אותה פריסה. **(ג) אומת לשאלת רועי:** `sendOrderBackupToGoogleScript` נקרא בשני המסלולים — וואטסאפ (app.js ~971) ומייל (~999) — ולכן פרטי הלקוח מגיעים גם בשליחת וואטסאפ, ואפילו אם הלקוח לא ילחץ "שלח" בוואטסאפ (רשת ביטחון להזמנות נטושות). **(ד) פיד Merchant:** נוספו `g:google_product_category` (מיפוי 6 קטגוריות ה-care לטקסונומיה של גוגל — Hair Care / Oral Care / Laundry Supplies וכו', דרך `GOOGLE_PRODUCT_CATEGORIES` ב-server.py) ו-`g:product_type` — משפר סיווג והתאמה לחיפושים. **תובנה שנמסרה לרועי:** רישומים חינמיים ב-Merchant נותנים חשיפה מוגבלת; הנפח האמיתי ב-Shopping מגיע מקמפיין בתשלום, ולפני הכל צריך לוודא שהמוצרים **מאושרים** (החוסמים הנפוצים: הגדרות משלוח, אימות דומיין, ואי-התאמת מחיר בין הפיד לאתר).

29. **שכבת המוצרים B2B + CTR + המרה (1.8.26):** מסמך עיצוב: `docs/superpowers/specs/2026-08-01-b2b-product-layer-design.md`. רועי שלח 7 קובצי Search Console (3 חודשים) וביקש שיפורים רוחביים. **האבחון:** 75 קליקים על 1,627 הופעות, מיקום ממוצע 23. מ-11.7 ההופעות זינקו פי 8 אבל הקליקים נשארו ~1 ליום — הדפים במיקום 25–30. דפי הקטלוג יחד: 8 קליקים על 1,186 הופעות. **הפער המבני שאותר:** 176 מוצרי ה-B2B לא היו קיימים בגוגל — `catalog-shell.js` בונה את `#products` דרך `innerHTML` ולכן ב-HTML הסטטי אין אף שם מוצר; לא היה דף מוצר לאף מוצר; לכל 176 המוצרים `description` ריק; סכמת ItemList הייתה ידנית וכבר נסחפה (ניקיון 38 מול 39, נייר 17 מול 19).
    **(שלב 1)** `PRODUCT_FIELDS` ב-server.py כמקור אמת יחיד — `handle_save_products` היה גוזר עמודות מהמוצר הראשון, מה שמחק עמודה שלמה אם המוצר הראשון חסר אותה. עמודות `packaging` ו-`subcategory` + שדות בעורך (מחווטים בשלושת המקומות ב-admin.js — **כל שדה שלא נרשם שם נמחק בשמירה**). **176 תיאורים** נכתבו (רועי לעבור עליהם בעורך). **תוקן מזהה כפול `מוצ004`** ששימש גם ל"גליל תעשייתי פרח 1200" וגם ל"נייר קרם" → `יצי004`; שני מוצרים באותו מזהה שוברים deep links ואת מפת ה-slugs. `generate_catalog_category_schema()` מסנכרן ItemList מה-CSV (מפרסר את ה-JSON ומחליף רק את mainEntity — סימוני HTML היו שוברים את ה-JSON).
    **(שלב 2)** `generate_catalog_product_pages()` — **176 דפי מוצר** ב-`catalog/product/<slug>/` עם Product+BreadcrumbList, **בלי Offer** (המחירון מוסתר → אין rich snippets, הדף מדורג על הטקסט), שני CTA באותה בלטות. `bake_catalog_product_index()` — בלוק סטטי בכל דף קטגוריה **מחוץ ל-`<catalog-shell>`**; זו גם הדרך היחידה שגוגל יגיע לדפי המוצר. `_cleanup_stale_product_pages` ו-`_update_sitemap_care_products` הוכללו לקבל base/סימונים כדי ששני עצי המוצרים לא ידרסו זה את זה.
    **(שלב 3)** 5 דפי תת-קטגוריה לפי הופעות: `paper/hand-towels` (36), `paper/toilet-paper` (30), `disposables/containers` (22), `disposables/cups` (20), `disposables/tableware` (5). **השיוך דרך עמודת `subcategory` ולא regex** — התאמה לפי מילים ייצרה false positives ("סבון ידיים" ל"נייר ניגוב ידיים", "עט לחיץ בקופסא" ל"קופסאות"). `/catalog/bags/` לא קיבל דף — כבר קיים במיקום 11.55.
    **(שלב 4)** CTR: `/contact/` (157 הופעות במיקום 3.63, CTR 0.64%) ו-`/about/` (121, מיקום 3.49) — Title עם טלפון/הצעת ערך; **`/catalog/` לא היה בו H1 בכלל ורק ~50 מילים → 547 מילים ו-24 קישורים פנימיים**; Title לדפי מרחק-נגיעה לפי השאילתות (cleaning→"שיווק חומרי ניקוי" 126 הופעות, paper→"למוסדות", disposables→"לעסקים ולמסעדות", office-supplies→"סיטונאי"); "B2B" (מיקום 5.77) שולב.
    **(שלב 5)** מידע אריזה בכרטיס ובתצוגה המהירה (שורת האריזה נוצרת דינמית ב-app.js ולא כאלמנט HTML, כדי לא לחייב אותו מזהה בארבעה קבצים); **"הוסף הכל לסל"** בהזמנות אחרונות (מדלג על מוצרים שנמחקו); **אירועי GA4 `view_item`/`add_to_cart`/`begin_checkout`** דרך helper `trackEvent` — עד כה דווח רק `order_sent`, כלומר אפס מידע על מה שקורה לפני השליחה; `add_to_cart` נשלח רק במעבר מ-0 לכמות. תוקן `.highlight-product` ש-`.product-card` ב-style.css דרס.
    גרסאות: app.js?v=31, style.css?v=21, sw cache-v19. sitemap: 283 כתובות. אומת בדפדפן: round-trip בעורך, 1,208 קישורים פנימיים תקינים, אפס מזהים כפולים, sitemap תקין, שמות עם גרשיים ועם % נפתרים ב-200, `add_to_cart` נורה פעם אחת גם אחרי 3 לחיצות, קטלוג הטיפוח ודף הסוכן לא נפגעו, אפס שגיאות קונסול. **לא אומת ויזואלית:** צבע מסגרת ההדגשה — סביבת התצוגה החזירה `getComputedStyle` קפוא (גם inline `!important` לא נרשם).
    **דורש רועי:** push; מעבר על 176 התיאורים בעורך; Search Console (sitemap + אינדוקס לדפי המוצר והתת-קטגוריה); **Google Business Profile וקישורים נכנסים — המנוף האמיתי**, כי שלבים 1–3 מזיזים ממיקום ~25 ל~12–15 אבל מעבר לעמוד 1 דורש סמכות דומיין; מילוי הדרגתי של עמודת האריזה.

30. **שלב 0 — הכשרת /care/ למכירה ללקוח פרטי (2.8.26):** מסמך עיצוב: `docs/superpowers/specs/2026-08-01-care-b2c-checkout-design.md`. רועי ביקש לחשוב איך מביאים לקוחות שיקנו מקטלוג הטיפוח. **האבחון: זו לא בעיית אתר אלא בעיית הפצה** — לקטלוג אין מקור תנועה (נחיתה אורגנית אחת ב-30 יום). **התובנה האסטרטגית:** 45 מ-63 המוצרים (קולגייט, אריאל, ג'ילט, אולוויז, פרסיל) נמכרים בכל סופר ואי אפשר לגייס עליהם — הם ממלאי סל; 26 מוצרי השיער (ARGANIA ₪29.9-39.9, LA BEAUTE ₪54.9-69.9) הם מותגי מספרה שאין להם תחליף במחיר הזה. **מגייסים על שיער, מרוויחים על הסל** — ולכן מנגנוני הסל (הנחת ₪350 הקיימת + סף משלוח חינם) הם לב ההמרה. **החלטות רועי:** אספקה = מסלול חלוקה + עד הבית + לוקר + איסוף עצמי; תשלום = מזומן/אשראי במסירה **וגם** ביט/פייבוקס מראש, **בלי סליקה באתר**; דמי משלוח משתנים לפי שיטה ולכן ניתנים לעריכה; היקף = שלב 0 בלבד, קמפיינים בסבב הבא (גישה א+ב: מודעות Meta מקומיות על קו השיער + Google Shopping).
    **שני חוסמי המרה שנמצאו בקוד:** (1) טופס ההזמנה ב-`/care/` ביקש **"שם העסק / המוסד"** כשדה חובה — שדה B2B בעמוד B2C; (2) `buildMessage` גידר את כל בלוק פרטי הלקוח על `customer.business`, כך שבמצב B2C **כל פרטי הלקוח היו נעלמים מהודעת הוואטסאפ**. שניהם תוקנו.
    **מלכודת מאומתת:** `getCustomerDetails()` מחזיר `null` כש-`cust-business` חסר (הגנה לדף הסוכן) — מחיקה תמימה של השדה הייתה משביתה בשקט את כל טופס הלקוח. העיגון שונה ל-`cust-business || cust-contact`.
    **מה נבנה:** `data/care-shipping.json` + `_load_shipping()`/`_clean_shipping_methods()` + `POST /api/shipping` ב-server.py (בדפוס `/api/promo`), עם ברירת מחדל בטוחה — **לוקר ומשלוח עד הבית כבויים** עד שרועי ימלא מחירים, כדי שהאתר לא יבטיח שירות שלא קיים; כרטיס "שיטות אספקה ודמי משלוח" בעורך המחירון (ולידציה: "חינם מעל" בלי דמי משלוח נחסם); ב-app.js — `CONFIG.customerMode` ו-`CONFIG.shippingSource` (ברירת מחדל `'b2b'`/`null`, כלומר הקטלוג העסקי והסוכן לא נגעו), בוררי אספקה ותשלום במודאל, `orderTotals()` כמקור אמת יחיד, ונאדג' "עוד X ₪ למשלוח חינם" **רק במודאל**; כתובת מוצגת רק בשיטות משלוח; רצועת אמון ב-`/care/`; `g:shipping` בפיד (**רק שיטות `needs_address` — איסוף עצמי ולוקר אינם משלוח**, והמחיר הוא הדמים המלאים ולא הסף המוזל, לפי מדיניות גוגל). סדר החישוב: `סכום → הנחת סל → + דמי משלוח`.
    **תוקן אגב אורחא:** ה-webhook שלח לגיליון את **סכום הביניים** כ-`totalPrice` ולא את הסכום הסופי — כלומר עמודת "סה"כ לתשלום" התעלמה מהנחת הסל. עכשיו נשלח הסופי + פירוק (`subtotal`/`discount`/`shipping`). `tools/google-apps-script-orders.gs`: שלוש עמודות חדשות **נוספו בסוף השורה ולא באמצעה** כדי שהגיליון הקיים של רועי לא יזוז, + שדרוג כותרות אוטומטי לגיליון קיים.
    **תקנון:** נסגרו 2 מתוך 4 ה-`[להשלמה]` (אמצעי תשלום, דמי משלוח). **עדיין פתוחים ודורשים את רועי: מספר עוסק ומדיניות החזרות מדויקת (טעון עו"ד).**
    גרסאות: app.js?v=32, sw cache-v20 (style.css לא שונה — כל ה-CSS החדש inline ב-care). **אומת בדפדפן מקצה לקצה:** 169+29.90=198.90; מעל סף 250 המשלוח מתאפס; 422.50 → הנחה 42.20 → 380.30; ההודעה זהה למוצג בעמוד; ולידציה "נא למלא שם מלא וטלפון" ממקדת ל-`cust-contact`; כתובת נעלמת באיסוף עצמי; פיילוד ה-webhook תואם לסקריפט; **רגרסיה: `/catalog/` שומר על "שם העסק *" ואפס בוררי משלוח, `/agent/` תקין** (כפתורי השליחה שם מוסתרים ממילא ב-`allowShare:false` — לא רגרסיה); מובייל 375px בלי גלישה; round-trip בעורך; פיד 62 פריטים עם `g:shipping`; sitemap 283 כתובות ללא שינוי. נתוני הבדיקה נוקו — הקונפיג חזר לברירת המחדל הבטוחה.
    **דורש רועי:** push; **למלא בעורך דמי משלוח אמיתיים ללוקר ולעד-הבית ולהפעיל אותן** (עד אז מוצגים רק מסלול חלוקה ואיסוף עצמי); מספר עוסק ומדיניות החזרות בתקנון; התקנת **גרסה 3** של סקריפט ההזמנות; **הסבב הבא = הקמפיינים** (Meta מקומי על קו השיער + Shopping על הפיד המאושר).
    **פער ידוע שלא טופל (מכוון):** `aroam_customer_details` ב-localStorage אינו מופרד לפי קטלוג, כך שפרטים שהוזנו ב-`/care/` ממלאים מראש גם את טופס ה-B2B (התנהגות שקדמה לסבב; ה-CART_KEY כן מופרד).

31. **תצוגת סל מלאת-מסך עם כתובת משלה (2.8.26):** מסמך עיצוב: `docs/superpowers/specs/2026-08-02-cart-view-design.md`. רועי ביקש להחליף את חלון "סיכום הזמנה" בדף, משתי סיבות שהוא סימן: **החלון נראה צפוף** (אחרי סבב 30 הוא מכיל טבלה + בורר אספקה + בורר תשלום + טופס לקוח + נאדג'ים — 150 שורות HTML) ו**רצון לדעת מי נוטש את הסל**.
    **ההחלטה — תצוגה מלאת-מסך באותו עמוד, לא דף נפרד.** מבחינת הלקוח זהה (מסך מלא, כתובת, "אחורה" עובד), אבל דף נפרד (`/catalog/cart/`) היה מחייב טעינה מחדש של products.csv+מבצעים+משלוחים כדי לרנדר את הסל — השהיה בדיוק ברגע הכוונה הגבוהה ביותר — וגם שכפול הדר/פוטר ומצב "סל בלבד" ב-app.js. **היקף: `/catalog/` + 10 דפי הקטגוריה + `/care/`. דף הסוכן נשאר על המודאל הישן** (שם הוא משרת גם הצעות מחיר, PDF והיסטוריה).
    **התנגשות שנמצאה בזמן הביצוע:** התכנון היה `?cart=1`, אבל **הפרמטר `cart` כבר תפוס** לשיתוף עגלה בפורמט `cart=id:qty,id:qty` (כפתור "שתף קישור"). הפרמטר שונה ל-**`?view=cart`**. אומת ששיתוף העגלה ממשיך לעבוד ואינו פותח את התצוגה.
    **מלכודת שטופלה:** `orderModal` נסגר בלחיצה על הרקע (`e.target === orderModal`) — בתצוגת מסך מלא הרקע הוא כל המסך, כלומר כל לחיצה הייתה סוגרת את הסל. ההתנהגות מנוטרלת כש-`.cart-page` פעיל, ונשמרת כרגיל בדף הסוכן.
    **מה נבנה:** `CONFIG.cartView` (ברירת מחדל `false`); `openOrderModal` מוסיף `.cart-page` + `history.pushState`; `closeOrderView()` כנתיב סגירה יחיד (כולל אחרי שליחה — מנקה את הפרמטר); מאזין `popstate`; פתיחה מ-URL באתחול עם **הגנת עגלה ריקה** (הפרמטר מוסר כדי שקישור ישן לא ינחית על סל ריק); כפתור "→ המשך בקנייה" נבנה דינמית וה-× מוסתר. **CSS:** כל הכללים מגודרים תחת `.cart-page` ב-style.css — מסך מלא, גריד דו-טורי ≥1024px (טבלה מימין ב-`grid-row: 1/span 99`, כל השאר נערם בטור השני), הדר/גוף/פוטר מיושרים לאותה עמודה 1200px דרך `padding-inline: max(...)`, ובר פעולה דביק. **מבנה ה-DOM לא שונה כלל** — ולכן 10 דפי הקטגוריה, שמקבלים את המודאל מ-`catalog-shell.js`, עבדו בלי לגעת בהם.
    **מדידה:** `view_cart` + `page_view` וירטואלי. ה-`page_path` הוא **נתיב קנוני** (`pathname + '?view=cart'`) ולא הכתובת בפועל — אחרת כל קישור עגלה משותפת היה הופך לעמוד נפרד בדוח ומפצל אותו. המשפך עכשיו: `view_item` → `add_to_cart` → **`view_cart`** → `begin_checkout` → `order_sent`.
    גרסאות: app.js?v=33, style.css?v=22, sw cache-v21. **אומת בדפדפן:** URL משתנה ל-`?view=cart`; "אחורה" סוגר את הסל ונשאר ב-`/catalog/`; רענון על `?view=cart` משחזר 5 שורות; עגלה ריקה → הפרמטר מוסר והסל לא נפתח; לחיצה על רקע לא סוגרת; גריד `683px 441px` בדסקטופ ועמודה אחת ב-375px בלי גלישה; שלושת אירועי GA4 נורים; שליחה מהתצוגה תקינה וההודעה תואמת (164.60); דף קטגוריה עם `presetCategory` תקין; **רגרסיה `/agent/`: אין `.cart-page`, הכתובת לא משתנה, ה-× גלוי, max-width 800px, רקע כהה, סגירה בלחיצה על הרקע עובדת**. אפס שגיאות קונסול.
    **דורש רועי:** push; ב-GA4 לסמן `view_cart` כאירוע למעקב ולבנות קהל רימרקטינג "פתחו סל ולא הזמינו" — זה הקהל שישמש בקמפיינים.

32. **עיצוב עמוד הסל — כרטיס סיכום ושורות מוצר (2.8.26):** רועי העיר שהעמוד "נראה ריק ולא מקצועי". **אבחון:** (1) "סה״כ לתשלום" ישב בפינת הפוטר באותו משקל ויזואלי כמו "סה״כ פריטים" — המספר החשוב ביותר בעמוד היה קבור; (2) אין היררכיה, הכל שטוח על `--bg-body`; (3) `grid-row: 1/span 99` + `align-items:start` השאיר ~60% מהמסך לבן; (4) זו טבלת נתונים (מק״ט/קטגוריה/יחידה, תמונות 40px) ולא סל קניות; (5) **הפרות טוקנים שאני הכנסתי בסבב 31** — `#fff` ו-`0 -2px 12px rgba(...)` קשיחים; (6) אין מצב סל ריק.
    **ההחלטה — טיפול שונה לכל קטלוג, כי הקהל שונה:** `/catalog/` = מנהל רכש שמזמין עשרות מק"טים, ולכן **הטבלה הקומפקטית היא יתרון ונשמרת**; `/care/` = לקוחה פרטית עם 4 מוצרים, ולכן שורות מוצר עשירות עם תמונה. **אילוץ מכריע שאותר:** בקטלוג העסקי `showPrices:false` — כרטיס סיכום עם סכומים היה חסר משמעות שם, ולכן הוא מציג ספירת פריטים בלבד.
    **`#cart-summary-card`** (`renderCartSummaryCard`/`updateCartSummaryCard`, מתעדכן מ-`updateSummary`): כשיש מחירים — סכום ביניים/הנחה/דמי משלוח/סה״כ גדול; אחרת ספירה. בדסקטופ `sticky` + `order:-1` בטור הצדדי, וזה מה שסוגר את החלל המת. **מניעת כפילות:** בדסקטופ הכרטיס נושא את ה-CTA ו**הבר התחתון מוסתר**; במובייל הפוך — ה-CTA בכרטיס מוסתר והבר נשאר. כפתורי הכרטיס **מפעילים את הכפתורים הקיימים** (`send-whatsapp`/`send-email`/`clear-cart-btn`) כדי שלוגיקת השליחה תישאר במקום אחד.
    **`.cart-rich`** (נוסף רק כש-`cartView && showPrices`, כלומר care בלבד): **שימוש חוזר במבנה הכרטיסים של סבב 23** בכל רוחב מסך — גריד `"img name name"/"img price price"/"img qty total"`, תמונה 72px. **אפס שינוי ב-`fillOrderTable`** — רק CSS על ה-classes `oc-*` הקיימים. **`#cart-empty`** — מצב סל ריק עם חזרה לקטלוג, וה-CTA מקבל `disabled`.
    **תיקון אחרי משוב של רועי (צילום מסך אמיתי):** שלושה באגים שהאימות הגיאומטרי שלי החמיץ כי בדק תכונות (sticky/display/אזורי גריד) ולא **מיקומים אנכיים** אחרי השינוי. (1) הטבלה הועברה ל-`grid-row: 2/span 98` כדי לפנות מקום למצב הריק — וכך **שורה 1 בטור הראשון נשארה ריקה** והטבלה נדחפה מתחת לכרטיס במקום לצדו; הוחזר ל-`1/span 99`. (2) `sticky top: 5.5rem` על הכרטיס — הגולל הוא `.modal-body` וההדר יושב **מחוצה לו**, ולכן לא היה מה לפנות: הערך הצמיד את הכרטיס 88px כלפי מטה כבר בגלילה 0; הוקטן ל-`0.25rem`. (3) במצב סל ריק הטבלה ומצב-הריק חולקים את אותו תא בגריד, ושורת הכותרות נערמה מעל ההודעה; הטבלה מוסתרת עכשיו יחד עם המעבר למצב ריק.
    גרסאות: app.js?v=35, style.css?v=24, sw cache-v23. אומת מחדש: כרטיס וטבלה מיושרים (28 מול 24), מצב ריק בלי חפיפה עם CTA `disabled`, care עם שורות עשירות ופוטר מוסתר, `/agent/` ללא כרטיס/מצב-ריק/`cart-page` ובמודאל 800px, אפס שגיאות קונסול. **אומת (גיאומטרית ב-DOM — לכידת צילומי המסך בסביבה הייתה לא אמינה ברוחב גדול):** care דסקטופ — כרטיס sticky, פוטר `display:none`, גריד השורות עם 5 אזורים ו-sku מוסתר; care מובייל — CTA בכרטיס מוסתר והפוטר גלוי, אפס גלישה אופקית; catalog — `cart-rich` לא מוחל, השורות נשארו `table-row` עם sku גלוי, הכרטיס מציג "סה״כ פריטים 14"; מצב ריק — CTA `disabled`; **רגרסיה `/agent/`: אין כרטיס, אין מצב ריק, אין `cart-page`/`cart-rich`, מודאל 800px עם רקע כהה, פוטר גלוי**. אפס שגיאות קונסול.

33. **עמוד הסל — צבעי המותג ורצועות האתר (2.8.26):** רועי אמר שאהב את שילובי הצבעים במוקאפים שהוצגו לו וביקש "לשלב עוד צבעים" ולהוסיף את **השורות העליונות שיש בכל האתר**. **מה שהיה:** רקע `--bg-body` אפור שטוח, והתצוגה מלאת-המסך הסתירה את כל הכרום של האתר.
    **צבע:** רקע העמוד עבר ל-`--primary-light` (‎#EAF3EE) — הוא מה שנותן לכרטיסים הלבנים עומק; **במצב כהה הטוקן כבר מוגדר כ-‎#1A4231 ולכן זה עובד בשתי הערכות בלי כלל נוסף.** נוסף `--primary-pastel: #8FD6A3` **כטוקן ב-:root** במקום הערך הקשיח שהשתמשתי בו. הצבע הוחל: כותרת כרטיס הסיכום כרצועה ב-`--primary-dark`, שורת הסה"כ על `--primary-light`, פס `border-inline-start` ירוק בשורות המוצר, `thead` של הטבלה העסקית בגוון המותג, ומסגרת תחתונה ירוקה לכותרת.
    **כרום:** `renderCartChrome()` (החליף את `renderCartBackButton`) מזריק **רצועת utility בירוק כהה** עם הטלפון ואזור החלוקה — אותם צבעים בדיוק כמו `.utility-bar` ב-site.css — ואת **הלוגו** בכותרת. במובייל טקסט האזור מוסתר והלוגו קטן ל-34px. גושי הטופס (`#customer-details`, `#fulfillment-options`) הפכו לכרטיסים לבנים, כי הרקע הירקרק ה-inline שלהם נבלע ברקע הפסטלי.
    **באג שנמצא אגב אורחא ותוקן:** `.oc-price` בטבלת ההזמנה הוא **שדה קלט ניתן לעריכה** — פיצ'ר של הסוכן להצעות מחיר, שדלף גם ל-`/care/`. לקוח שערך את המחיר ראה את שורת הסכום משתנה (21.80→2.00) **בעוד שסה"כ הסל וההודעה הנשלחת נשארו נכונים** — כלומר אין ניצול כספי, אבל המספרים על המסך סתרו זה את זה בעמוד תשלום. נוסף `CONFIG.editablePrices` (ברירת מחדל **false**) ו-`editablePrices: true` הוגדר במפורש ב-`agent/index.html`; מחוץ לסוכן השדה `readOnly` עם `tabIndex=-1` ומוצג כטקסט עם התווית "מחיר ליחידה:".
    **תיקון אחרי משוב נוסף:** רועי דיווח שכרטיס הסיכום **דורס את טופס פרטי הלקוח בגלילה**. שוחזר ונמדד: בגלילה 500 תחתית הכרטיס ב-475 בעוד ראש הטופס ב-396. **הסיבה מבנית ולא ויזואלית** — הכרטיס והטופס חלקו את אותו טור, וכרטיס `sticky` בהכרח מכסה את מה שמתחתיו באותו טור. **הפתרון הוא זה שצ'קאאוטים אמיתיים עושים:** כל התוכן (מוצרים, תאריך, אספקה, פרטי לקוח) עבר לטור הראשי, והטור הצדדי שמור לכרטיס בלבד עם `grid-row: 1 / span 99` + `align-self: start` — כך שטח הגריד שלו משתרע על גובה העמודה ו-sticky עובד בלי לדרוס דבר. אומת: אפס חפיפה ב-0/500/900/1200 ובתחתית, הכרטיס נשאר גלוי (top קבוע 28) בכל מצבי הגלילה, ובמובייל הוא `static` כך שאין השפעה.
    גרסאות: app.js?v=37, style.css?v=27, sw cache-v26. **אומת:** צבעים (רקע ‎#EAF3EE, רצועה ‎#1A4231 עם טקסט ‎#DFE9E3, פסים ‎#639C7D, אקסנט ‎#8FD6A3); care דסקטופ ומובייל 375px בלי גלישה; catalog — כרום קיים, כרטיס וטבלה מיושרים, אין תאי מחיר; **`/agent/` — עריכת מחיר עדיין עובדת (15→20 ⇒ 40.00), בלי רצועה, בלי כרטיס, מודאל 800px עם רקע כהה**; אפס שגיאות קונסול.

34. **תשתית שיווק ממומן — סבב 1 (2.8.26):** מסמך עיצוב: `docs/superpowers/specs/2026-08-02-paid-marketing-design.md`. רועי ביקש תוכנית פרסום ממומן **בדגש על הקטלוג העסקי ולא על `/care/`**. נתוני היסוד שמסר: **₪1,500/חודש**, מטרות מבוססות-לידים (טלפון/וואטסאפ/טופס/פגישה), הזמנה ₪500–1,500 חוזרת, **מחירים נשארים מוסתרים**, מנהל את החשבון בעצמו, אזור **מיבנה עד רעננה ללא ירושלים**, מיקוד ב**חד פעמי ואריזות + מוצרי נייר**, וזמין כמעט תמיד לענות.
    **ההכרעה — 100% Google Search בשלושת החודשים הראשונים:** מחירים מוסתרים שוללים Shopping ו-PMax (אין פיד B2B — `meta-catalog-aroam.csv` הוא של care בלבד); ₪50/יום לא מתחלק בין ערוצים בלי להשאיר את שניהם בשלב הלמידה; ומטא לא מתאים כערוץ ראשון ב-B2B — הוא ייכנס לרימרקטינג בחודש 4 אם הקהל יעבור 1,000 ב-30 יום.
    **חמישה חסמים שאותרו בקוד:** (1) **אפס `AW-` ואפס `fbq`** באתר — רק GA4 ב-106 דפים; (2) פיילוד טופס הלידים לא כלל **כתובת עמוד, UTM או gclid**, כלומר ליד נכנס לגיליון בלי דרך לדעת איזו מודעה הביא אותו; (3) **176 דפי `catalog/product/` ו-5 דפי תת-קטגוריה נוצרו בלי gtag כלל** (דפי `care/product/` דווקא כן קיבלו); (4) דפי הקטגוריה הם רשת מוצרים בלי מחיר ובלי טופס — יעד גרוע לקליק בתשלום; (5) בדפי הפלחים הטופס יושב סביב שורה 264, כ-4–5 גלילות במובייל.
    **מה נבנה:** **`lp/disposables/` ו-`lp/paper/`** — `noindex,nofollow` כדי שלא יתחרו בדפי הקטגוריה האורגניים (לא ב-sitemap/llms, **ובכוונה לא ב-robots.txt** כי חסימת סריקה הייתה מונעת מגוגל לראות את ה-noindex), טופס בהירו במבנה `1fr 460px` שנראה במלואו בלי גלילה, `_subject` עם "(ממומן)", וה-CSS המשותף הועבר ל-`site.css` במקום inline. **`captureAttribution()`** ב-components.js שומר UTM/gclid ב-`sessionStorage` בגישת **first-touch** (נדרש כי הגולש מנווט לפני שממלא), והפיילוד התרחב ב-`עמוד`/`דף נחיתה`/`מקור`/`gclid`. **בר CTA דביק במובייל** מוזרק לפי `data-cta-bar` (2 LP + 4 דפי פלחים); הוא משתמש ב-`tel:`/`wa.me` ולכן **נתפס במאזין ההמרות המואצל הקיים בלי קוד מעקב חדש**, הוואטסאפ הצף מוסתר במובייל כשהבר קיים (כפילות), וצבעי הכפתורים קבועים ולא `--secondary-color` — **שהופך ללבן במצב כהה**. **gtag נוסף ל-`generate_catalog_subcategory_pages()` ול-`_render_catalog_product_html()`** ב-server.py; **כיסוי ה-gtag עלה מ-106 ל-289 דפים.**
    sw cache-v27. **אומת:** דסקטופ 1280×860 — גריד `656px 460px` והטופס כולו מ-168 עד 696 בלי גלילה; מובייל 375px — בר נעוץ (741→812), הוואטסאפ הצף ו-`.lp-direct` מוסתרים, כפתור נגישות ב-88px בלי חפיפה, אפס גלישה, טופס ב-0.73 מסך; שני כפתורי הבר יורים `phone_click`/`whatsapp_click`; פיילוד הטופס (עם fetch ממותג) הכיל `מקור: "disposables | test-kw | google"` ו-`gclid`, והייחוס שרד ניווט בין הדפים; `data-cta-bar` ב-6 דפים בלבד; **רגרסיה `/catalog/` — אין בר, `.fab-container` קיים, נגישות ב-78px, 22 מוצרים**; `refresh_site.py` רץ ו-**כל 181 הדפים השתנו ב-+2/−0 שורות בדיוק** בלי סחף; אפס שגיאות קונסול.
    **דורש רועי:** (1) **לפתוח חשבון Google Ads ב-Expert Mode ולמסור מזהה `AW-`** — חוסם את מדידת ההמרות; (2) push; (3) **לוודא שסקריפט הלידים (`AKfycbxfp0q…`, לא נמצא ב-`tools/`) כותב מפתחות דינמיים** אחרת שדות הייחוס ייבלעו; (4) לבנות את הקמפיינים לפי המסמך — **הכי חשוב: לבטל Display ושותפי חיפוש, להעביר Location ל-`Presence`, לכבות auto-apply, ולהוסיף את שלילות "חד פעמי" הפיננסי (מענק/תשלום/הלוואה) ושל ימי הולדת**; להתחיל ב-₪25/יום שבוע ראשון.

35. **עמוד ועד עובדים — קטלוג סגור עם מחירון נפרד (5.8.26):** מסמך עיצוב: `docs/superpowers/specs/2026-08-05-committee-catalog-design.md`. נקודת שחזור: `ae16564`. רועי ביקש עמוד מכירה סגור לוועד עובדים של חברה אחת, עם מוצרי הטיפוח + מוצרים נבחרים מהקטלוג הראשי, **במחירים אחרים**, נגיש רק דרך קישור.
    **החלטות:** כל עובד מזמין לעצמו · כתובת סודית + קוד כניסה · מחירון עצמאי מלא · משלוח עד הבית ₪30 / לוקר ₪20, **חינם מעל ₪300** (אין איסוף עצמי) · תשלום באשראי/ביט **שרועי גובה טלפונית** (אין סליקה באתר) · וועד אחד בלבד · פתוח קבוע · **מיתוג ניטרלי "מבצע לעובדים" בלי שם החברה** — כך שדליפת הקישור לא חושפת למי ניתנו מחירים מיוחדים.
    **העמוד `emp-8c3f5a/index.html`** — נגזר מ-`care/index.html` בלי שום בלוק SEO (אין סכמה, אין הזרקת מוצרים, אין canonical, אין דפי קטגוריה/מוצר, אין פיד): `noindex,nofollow` + `referrer: no-referrer`, לא ב-sitemap/llms, **ובכוונה לא ב-robots.txt**. **שער כניסה** בדפוס `agent/index.html` עם שני שיפורים: הקוד נשמר ב-`localStorage` (`aroam_emp_ok`) כדי שעובד לא יקליד בכל כניסה, ופרמטר **`?k=<קוד>`** פותח ישירות **ומיד נמחק מהכתובת** ב-`replaceState` כדי שלא ידלוף בצילום מסך. הקוד הנוכחי **`7241`**, מוגדר בקבוע `ACCESS_CODE` בראש הסקריפט בעמוד.
    **נתונים:** `data/committee-master.csv` (מקומי, ב-`.gitignore`, עם עלות) → `data/committee.csv` הציבורי, בתוספת `committee-promo.json` ו-`committee-shipping.json`. מזורע מ-63 מוצרי הטיפוח **במחירי הטיפוח** — רועי מעדכן אותם.
    **`app.js` — שני דגלים אדיטיביים בלבד:** `CONFIG.orderPrefix` (ברירת מחדל `'AR'`) ⇒ הזמנות הוועד הן `VD-20260805-1234` **ולכן מסוננות בגיליון בלי לגעת ב-Apps Script**; `CONFIG.paymentOptions` עוקף את `PAYMENT_OPTIONS`. **הומר לפונקציה `paymentOptions()`** ולא לקבוע — קבוע היה נקרא לפני ש-`init()` מציב את `CONFIG` (תלות בסדר הערכה).
    **העורך — פרמטריזציה, לא שכפול** (`admin/pricelist.js`, 1,126 שורות): מפת `CATALOGS` + `?catalog=` בכתובת קובעת קבצים, endpoint ומפתח גיליון הגוגל (**מפתח נפרד לכל קטלוג — הגיליון החדש לא דורס את זה של הטיפוח**); מתג "מחירון טיפוח / מחירון ועד" ב-topbar; **כפתור "ייבוא מהקטלוג הראשי"** שקורא את `products.csv`, מציג בחירה מרובה עם חיפוש ותמונות, ומעתיק שם/קטגוריה/יחידה/תמונה/תיאור עם מחיר ריק. `server.py`: מפת `PRICELIST_CATALOGS` ב-3 ה-handlers; **שמירת הוועד לא מריצה אף גנרטור של care**.
    **סיכון שנמצא בבדיקה ותוקן:** מוצר מיובא בלי מחיר הוצג בעמוד כ-**"0.00 ₪" וניתן להזמנה**. נוספה רשת ביטחון ב-`handle_save_pricelist` — שורה בלי `price>0` נשמרת במאסטר אך **לא נכתבת לקובץ הציבורי**, ובטבלת העורך היא מסומנת "חסר מחיר". חל על שני המחירונים (ב-care אין היום אף מוצר כזה, כלומר אפס שינוי התנהגות).
    גרסאות: app.js?v=38 (14 דפים), sw cache-v28. **אומת בדפדפן מקצה לקצה:** שער (חסימה, קוד שגוי, קוד נכון, שמירה לרענון, `?k=` נכנס ומנקה את הכתובת); 63 מוצרים; ₪279.30 ⇒ משלוח 30 וסה"כ 309.30, נאדג' "עוד 20.70 ₪", מעבר ללוקר ⇒ 20 והכתובת נעלמת, ₪359.10 ⇒ "ללא עלות" והנאדג' נעלם; פיילוד ה-webhook (**מיורט — לא נשלחה הזמנת בדיקה לגיליון**) עם `VD-20260805-6465` ופרטי לקוח מלאים; הודעת הוואטסאפ זהה למוצג; **בידוד — 0 קישורים ל-B2B (רק תקנון/פרטיות/נגישות), 0 אזכורים ב-sitemap/llms/robots, אין קישור לעמוד מאף דף באתר**; העורך — 65 במאסטר מול 63 בציבורי, **ורק קבצי הוועד השתנו** (pricelist/feed/sitemap/care ללא שינוי); `refresh_site.py` לא נגע בקבצי הוועד; מובייל 375px בלי גלישה; **רגרסיה: `/care/` (63 מוצרים, 2 אפשרויות תשלום, שיטות האספקה שלו), `/catalog/` ("שם העסק *", בלי מחירים ובלי בוררי משלוח), `/agent/` (PIN, עריכת מחיר, בלי cart-page, מודאל 800px)**. אפס שגיאות קונסול. נתוני הבדיקה נוקו.
    **דורש רועי:** (1) **push**; (2) לעדכן את מחירי הוועד בעורך (`localhost/admin/pricelist.html?catalog=committee`) — כרגע הם מחירי הטיפוח; (3) לפתוח **גיליון גוגל חדש** לוועד, לשתף "כל מי שיש לו קישור", ולהדביק את המזהה בכפתור המשיכה (Shift+לחיצה); (4) לשקול להחליף את קוד הכניסה `7241`; (5) בגיליון ההזמנות — עמודה ידנית "שולם/ממתין", כי הגבייה טלפונית ואין לה תיעוד אוטומטי; (6) **לא לפרסם את הכתובת בשום מקום** — לא בפוטר, לא ב-sitemap, לא בוואטסאפ הכללי.

36. **תעודת הזמנה PDF + הפיכתה להצעת מחיר (20.8.26):** מסמך עיצוב: `docs/superpowers/specs/2026-08-19-order-pdf-quote-design.md`. תוכנית: `docs/superpowers/plans/2026-08-19-order-pdf-quote.md`. נקודת שחזור: `5989e39`. רועי ביקש שלצד גיליון ההזמנות יגיע גם **PDF מעוצב עם תמונות המוצרים**, ושאפשר יהיה להפוך הזמנה להצעת מחיר ולשלוח ללקוח.
    **ההכרעה על עריכת ההצעה:** PDF אינו פורמט עריכה — שינוי מחיר לא מעדכן סכומי שורה, סה"כ או מע"מ, וכל "טופס PDF" היה מייצר מסמך שהמספרים בו סותרים (בדיוק התקלה שתוקנה בסבב 33). במקום זה מנוצל מה שכבר קיים בדף הסוכן: `editablePrices`, `printQuote()` והפרמטר `?cart=`. הפיצ'ר הוא **קישור**, לא מנוע עריכה.
    **שלושה ספייקים בענן שינו את שיטת המימוש.** התוכנית המקורית בנתה HTML ונתנה לגוגל להמיר. הספייק גילה ש**הממיר HTML→PDF של גוגל מוחק את כל צבעי הרקע** — נבדקו חמישה אופנים (`background-color` על div/td/th, ותכונת `bgcolor` על table/td) וכולם נמחקו, כלומר הרצועה הירוקה הכהה הייתה נעלמת והטקסט הלבן שעליה הופך לבלתי קריא. גבולות, פינות מעוגלות, פסי צד, צבעי טקסט ו-flexbox דווקא שרדו. **הפתרון: לבנות את המסמך כ-Google Doc ולייצא ל-PDF** — שם רקע של תא הוא תכונה של הפורמט. ויתור יחיד: אין פינות מעוגלות. **ממצא שני:** `appendInlineImage` של Docs **אינו מקבל WebP**, ו-155 מ-176 תמונות המוצר הן webp — מה שהפך את תמונות ה-thumbs מרצויות ל**חובה**.
    **מה נבנה:** `tools/make_pdf_thumbs.py` (234 תמונות `images/thumbs/<מזהה>.jpg`, 120x120, 984KB, אידמפוטנטי, קורא משלושת קובצי המוצרים); ב-`tools/google-apps-script-orders.gs` — `orderColumns_` (מבנה טהור, נבדק ב-Node), `buildOrderDoc_`, `savePdf_`, `quoteLink_`; `tools/test/order-doc.test.js` (**10 בדיקות** — רתמה שטוענת את קובץ ה-`.gs` עצמו עם דמויות של שירותי גוגל, כדי שלא ייווצר עותק שני של הלוגיקה); עמודת **"קובץ PDF"** בסוף `SHEET_HEADERS` (`STATUS_COL` נשאר 12); ב-`js/app.js` — שדה `image` בפיילוד, פרמטר `?customer=` בדף הסוכן, ואזהרה גלויה על מזהים חסרים; ב-`css/style.css` — עיצוב הדפסת הצעת המחיר.
    **סדר העמודות הפוך לסדר הקריאה בקוד:** ב-Docs עמודה 0 מרונדרת בצד שמאל, ולכן `orderColumns_` בונה `[כמות, יחידה, מק"ט, מוצר, תמונה]` כדי שהתמונה תופיע מימין — בקשה מפורשת של רועי.
    **`product.image` אינו הנתיב הגולמי** — הוא עובר ב-`loadProducts` המרה לנתיב מקודד לתצוגה. לכן נוסף `rawImage` והוא זה שנשלח בפיילוד.
    **חמישה באגים נתפסו באימות בענן ולא היו מתגלים אחרת:** שורת המבצע הוכפלה (Docs מפצל `\n` בתא לפסקאות בעצמו, והקוד יצר אותן שוב); הקטגוריה נשמטה מתא המוצר (פער בתוכנית עצמה); הפוטר התנגש; הפס הירוק מעל הכרטיסים רונדר כרצועה שבורה (וּוּתר); ו**דליפת מסמכים ב-Drive** — `setTrashed` לא רץ כשהייצוא נכשל, כך שכל הזמנה כושלת הייתה משאירה Doc יתום (נסגר ב-`try/finally`). בנוסף **`MailApp.sendEmail` לא הייתה מוגנת** — כשל בצירוף ביטל את כל מייל ההתראה אף שהשורה כבר נכתבה; עכשיו יש ניסיון חוזר בלי הצרופה.
    גרסאות: app.js?v=41, style.css?v=32, sw cache-v34 (14 דפים). **אומת בענן על פרויקט וגיליון בדיקה נפרדים — לא על הפרויקט והגיליון החיים:** שתי שורות נכתבו, עמודה P עם קישורי Drive פעילים, העיצוב אושר ויזואלית ע"י רועי בשתי הגרסאות (עסקית 5 עמודות וטיפוח 7 עמודות עם מבצע וסיכום), וכשל מכוון במשיכת תמונות לא מנע כתיבת שורה. הדפסת הצעת המחיר אומתה ברינדור דרך מנוע הדפסה אמיתי.
    **דורש רועי:** (1) **push — חייב לקדום לפריסת הסקריפט**, כי `images/thumbs/` צריך להיות חי באתר לפני שהסקריפט בענן מושך משם תמונות; (2) הדבקת `tools/google-apps-script-orders.gs` בפרויקט "aroma order webhook" ופרסום **גרסה חדשה** על אותה פריסה (מזהה הפריסה לא משתנה, אין צורך לגעת ב-app.js); (3) הזמנת בדיקה אמיתית מהאתר ומחיקת השורה אחריה; (4) ניקוי קובצי הספייק והגיליון "בדיקה בלבד - הזמנות אהרוני" מה-Drive.

37. **ליטוש הצעת המחיר ותעודת ההזמנה (20.8.26):** מסמך עיצוב: `docs/superpowers/specs/2026-08-20-quote-polish-design.md`. הפיצ'ר של סבב 36 נפרס בפועל — רועי שלח הזמנת בדיקה מהאתר החי, קיבל את `AR-20260820-4437.pdf` במייל, ושלח צילום מסך שממנו עלו ליקויים.
    **מה שאותר בפועל:** (1) **הלוגו ב-PDF מעוות** — `setWidth(44).setHeight(20)` בעוד הקובץ הוא 182×109 (יחס 1.67), כלומר הוא נמתח לרוחב; (2) **שורות הסיכום ב-PDF הפוכות לעברית** — הסכום מימין והתווית משמאל, כי ב-Docs עמודה 0 מרונדרת בשמאל והקוד שם בה את התווית; (3) **"סה״כ לתשלום" בהצעה המודפסת הופיע אחרי שורות החתימה** — הסכום יושב ב-`modal-footer` וגוש החתימות מוזרק ל-`modal-body` שקודם לו (נמדד: חתימות ב-770, סה״כ ב-1106); (4) **שדה "תאריך הזמנה" הודפס כקלט תיבה ריק**; (5) **מספר ההצעה** היה `Q-1755612345678` — timestamp בן 13 ספרות על מסמך שהלקוח רואה.
    **הכרעות רועי:** להוסיף שורת **מע״מ 18%** להצעה; **עיגול לשקל שלם רק על הסה״כ הסופי** (המחירים בשורות נשארים מדויקים), דרך תיבת סימון ליד הסה״כ שנזכרת בין ביקורים; מע״מ **לא** נוסף לתעודת ההזמנה — היא מסמך פנימי.
    **`tools/google-apps-script-orders.gs`:** לוגו 64×38 ורוחבי הרצועה `80/230/225` (הסכום נשאר 535, הרצועה לא זזה); סדר עמודות הסיכום התהפך — הסכום לעמודה 0 והתווית לעמודה 1, רוחבים `135/400`.
    **`js/app.js`:** `CONFIG.quoteVat` (ברירת מחדל `false`, מופעל רק ב-`agent/index.html`) ו-`VAT_RATE = 0.18`; **`quoteTotals(net)` כמקור אמת יחיד** — **העיגול מוחל על הסה״כ והמע״מ נגזר ממנו בחזרה** (`vat = gross - net`), אחרת המסמך היה מציג שלוש שורות שאינן מסתכמות לסכום שכתוב בהן; `generateQuoteId()` בתבנית `Q-20260820-9280` כמו מספר ההזמנה; `printQuote` מזריק בלוק `.print-summary` בין הטבלה לחתימות ומוסיף `body.quote-print`.
    **מלכודת שנתפסה באימות:** הסתרת `#order-date-field` הסתירה גם את **תצוגת התאריך שיושבת בתוכו** — התאריך נעלם לגמרי מההדפסה במקום להופיע כטקסט. תוקן להסתרת `#order-date-input-wrap` (הקלט בלבד). בנוסף, משפט הפוטר "המחירים אינם כוללים מע״מ" **סתר את בלוק הסיכום החדש** והוחלף ל"הסכום הסופי כולל מע״מ" כשה-VAT פעיל; `.toast` הוסתר בהדפסה.
    **תיחום:** כל כללי ההדפסה החדשים תחת `body.quote-print`, וכל התוספות מותנות ב-`quoteVat` — `/catalog/`, `/care/` ו-`/emp-8c3f5a/` לא נגעו.
    גרסאות: app.js?v=42, style.css?v=33, sw cache-v35 (14 דפים). **אומת בדפדפן:** 60.00 → מע״מ 11.00 → 71.00 עם עיגול (שלוש השורות מסתכמות); בלי עיגול 70.80; המתג נזכר אחרי רענון; סיכום ב-683 מול חתימות ב-961; `modal-footer` מוסתר (אין כפילות); תאריך כטקסט "20/08/2026"; `?customer=` מוצע ב-prompt; מספר `Q-20260820-7033`; 10 בדיקות `order-doc.test.js` עוברות; **רגרסיה: `/catalog/` (22 מוצרים, אין תיבה/מע״מ/כפתור הדפסה), `/care/` (בוררי אספקה תקינים), `/emp-8c3f5a/` (שער פעיל)**.
    **פריסה בוצעה מסביבת Claude (20.8.26, 22:02):** רועי ביקש שאעשה זאת בעצמי, והתיקונים הוחלו על הפרויקט "aroma order webhook" דרך Chrome ופורסמו כ**גרסה 4** על אותה פריסה (`AKfycbz_rRjFdy2MqP…` — לא השתנה, אין צורך לגעת ב-app.js). **שתי מלכודות שנתפסו לפני ההדבקה:** (א) הקובץ ב-`tools/` מכיל `YOUR_SPREADSHEET_ID` כמציין מקום — הדבקה שלו כמות שהוא הייתה **משביתה את הכתיבה לגיליון**; המזהה החי הוא `1IidCbfHIGuifPdcaP9Fv7bO6TTTEbSMJnk_gtUcU-jE`. (ב) הקוד בענן **אינו זהה בייט-לבייט** לקובץ ב-`tools/` — אחרי הסרת הערות ורווחים נשאר פער של ~185 תווים שלא זוהה (רשימת הפונקציות, `isTrashed` והניסיון החוזר במייל **כן** קיימים בענן, כלומר הוא מעודכן פונקציונלית). לכן בוצעו **חמש החלפות מחרוזת נקודתיות** במקום הדבקת הקובץ המלא — כל אחת אומתה כמופע יחיד לפני ההחלפה, ואחריה אומת שהקוד החדש קיים והישן נעלם. **בעתיד: לא להדביק את הקובץ המלא לענן בלי לפתור קודם את הפער הזה.**
    **דורש רועי:** (1) **push** (אין credentials של GitHub בסביבת Claude — נבדק גם ה-keychain); (2) **הזמנת בדיקה אמיתית מהאתר** כדי לראות את ה-PDF החדש, ומחיקת השורה מהגיליון אחריה; (3) לוודא ששיעור המע״מ 18% נכון לתאריך השליחה.

38. **הקישור "פתח כהצעת מחיר" מעולם לא נבנה — תוקן (20.8.26):** רועי שלח הזמנת בדיקה מהקטלוג העסקי (`AR-20260820-4449`) ודיווח "אין שורה כזאת" במייל. **האבחון:** `quoteLink_` ו-`orderColumns_` הכריעו "האם להזמנה יש מחירים" לפי `Number(data.totalPrice) > 0` — אבל `sendOrderBackupToGoogleScript` שולח `totalPrice` **מחושב תמיד**, גם כש-`CONFIG.showPrices` כבוי, כי `getCartItemsData` קורא מחירים מ-`products.csv` (יש מחיר ל-118 מ-176 המוצרים). כלומר הזמנה מהקטלוג העסקי סווגה כ"מתומחרת", הקישור לא נבנה **אף פעם**, וה-PDF קיבל עמודות מחיר. **הבדיקה בסבב 36 לא תפסה את זה כי ה-fixture שלה הניח `totalPrice: ''`** — הנחה שלא מתקיימת בשום הזמנה אמיתית.
    **התיקון:** `js/app.js` שולח `showPrices: !!CONFIG.showPrices` בפיילוד, ו-`quoteLink_` בודק `data.showPrices === false` במקום לנחש מהסכום; **כשהשדה חסר (app.js ישן בקאש) חוזרים לתנאי הישן**, כך שדפדפן שלא רענן לא מקבל התנהגות שגויה. **החלטת רועי:** עמודות המחיר בתעודה **נשארות** גם בהזמנה עסקית — הוא רוצה לראות מחירי מחירון, והלקוח ממילא לא מקבל את הקובץ. לכן `hasPrices` לא שונה.
    נוספו 4 בדיקות ל-`tools/test/order-doc.test.js` (סה"כ 14) כולל fixture `B2B_PRICED` שמשקף את מה שהאתר שולח **בפועל**. גרסאות: app.js?v=43, sw cache-v36. **אומת בדפדפן בפיילוד מיורט (לא נשלחה הזמנת אמת):** `/catalog/` שולח `showPrices:false` עם `totalPrice:"260.00"`, `/care/` שולח `showPrices:true`; הרצת `quoteLink_` המתוקן על אותו פיילוד מייצרת קישור עם 4 הפריטים ושם הלקוח. **פריסה: גרסה 5 (20.8.26, 22:28)** על אותה פריסה.
    **תלות דו-צדדית — חשוב:** הקישור יופיע רק כשגם הסקריפט מעודכן (בוצע) **וגם האתר** שולח את השדה — כלומר **אחרי push**. עד אז הזמנות מהאתר החי ימשיכו להגיע בלי הקישור.

39. **הצעת המחיר בעיצוב תעודת ההזמנה (20.8.26):** רועי קיבל את הקישור לעריכה (סבב 38 עבד), אבל אמר שההצעה "יוצאת בפורמט אחר" וביקש שתיראה כמו התעודה שמגיעה אליו במייל.
    **מה שהיה:** ההדפסה הזריקה כותרת ובלוק סיכום מעל **טבלת העבודה של המודאל** — סדר עמודות אחר, בלי כרטיסי פרטים, בלי פסים לסירוגין ובלי פוטר. **מה שנבנה:** `buildQuoteDocHtml()` בונה מסמך עצמאי (`.quote-doc`) שמעתיק את מבנה `buildOrderDoc_` מה-Apps Script — רצועת כותרת, שני כרטיסים, טבלה עם תמונות ופסים, סיכום, חתימות ופוטר — ובהדפסה מוסתרים הכותרת, טבלת העבודה, שדה התאריך והפוטר של המודאל. כך אין יותר תלות בסדר העמודות של ה-DOM.
    **הפרט שקבע את הפריסה:** ב-Docs עמודה 0 מרונדרת **בצד שמאל**, ולכן בתעודה שנשלחת במייל הלוגו בשמאל, כרטיס "פרטי ההזמנה" בימין ופרטי הקשר בפוטר בימין. שלושת הבלוקים ב-CSS מקבלים `flex-direction: row-reverse` כדי להגיע לאותו סדר בדיוק, ו-`money()` הותאם ל-`money_` (`00.00 ₪` ולא `₪00.00`).
    **באג שנמצא אגב אורחא ותוקן:** עריכת מחיר יחידה בדף הסוכן עדכנה את שורת הסכום בלבד — `updateSummary` המשיך לקרוא את המחיר מ-`PRODUCTS`, ולכן **סה״כ הסל, שורת המע״מ והמסמך המודפס הציגו את המחיר הישן**. עכשיו העריכה נכתבת חזרה ל-`product.price` (רק כש-`editablePrices`), והמחיר חוזר מה-CSV בטעינה הבאה. אומת: 15→20 בשורה של 2 יחידות ⇒ שורה 40.00, סל 260→270, מע״מ 307→319.
    גרסאות: app.js?v=44, style.css?v=34, sw cache-v37. אומת בסימולציית print: 3 שורות, לוגו בשמאל, כרטיס ההזמנה בימין, פוטר עם פרטי קשר בימין, טבלת העבודה מוסתרת, 260 → מע״מ 47 → 307 מעוגל. `/catalog/` ו-`/care/` לא מכילים כפתור הדפסה ולא `quoteVat` — אפס השפעה.

40. **עמוד רשת חנויות — קטלוג סגור עם בחירת סניף (21.8.26):** תוכנית: `~/.claude/plans/tranquil-roaming-hippo.md`. נקודת שחזור: `06cd401`. רועי ביקש עמוד ייעודי ללקוח אחד שמפעיל הרבה חנויות בכמה מרכזי קניות: קוד כניסה, בחירת סניף במבנה **מרכז קניות ← חנות**, רק חלק מהמוצרים, ועריכה עצמאית של המרכזים, החנויות והמוצרים הגלויים.
    **החלטות רועי:** בלי מחירים · בלי בוררי אספקה/תשלום (משלוח לסניף, תשלום מול הרשת) · קוד אחד לכל הרשת · שם חנות בלבד (בלי כתובת/טלפון) · הטופס מבקש **שם המזמין (חובה) והערות** — הסניף הוא הזיהוי של החנות, והשם הוא של מי שהזמין · תמיד שתי רמות.
    **ההכרעה הארכיטקטונית — אין מחירון נפרד.** בעמוד הוועד היה צורך ב-`committee.csv` כי המחירים שונים; כאן אין מחירים, ולכן עותק שני של 176 המוצרים היה מתיישן בכל שינוי שם/תמונה. במקום זה `data/chain.json` מחזיק **רשימת מזהים** בלבד, והעמוד טוען את `products.csv` הרגיל ומסנן. מקור אמת אחד.
    **`js/app.js` — שלוש תוספות מגודרות בלבד:** (1) `CONFIG.allowedProductsSource` + `applyProductFilter()` שרץ **מיד אחרי `loadProducts` ולפני בניית הקטגוריות**, ולכן קטגוריה בלי מוצרים גלויים לא מופיעה כלל; **כל מסלול כישלון — קובץ חסר, JSON פגום, רשימה ריקה — משאיר את הקטלוג המלא**, כדי שהעמוד לא ייראה שבור לפני שהרשימה הוגדרה. (2) `customerMode: 'branch'` — אין שדות חובה, ו**לא נשמר** ל-`aroam_customer_details`: המפתח משותף לכל הקטלוגים (פער ידוע מסבב 30), ובלי הגידור שם הסניף היה מודלף לטופס של הקטלוג העסקי ולהפך. (3) ב-`buildMessage` התווית היא `סניף:` במקום `שם העסק:` — **השדה עצמו נשאר `business`, ולכן הפיילוד ל-webhook, הגיליון ותעודת ה-PDF לא השתנו כלל ולא נדרש עדכון בענן.**
    **העמוד `shops-4b7e2c/index.html`:** נגזר מ-`emp-8c3f5a` (כותרת ופוטר עצמאיים, בלי באנר ובלי מבצעים). שער הקוד זהה למנגנון הקיים כולל `?k=` שנמחק מהכתובת ב-`replaceState`; אחריו בורר סניף שנשמר ב-`aroam_shops_branch` ו**מאומת מול הקובץ בכל טעינה** — מרכז או חנות ששמם שונה מחזירים את הבורר. כשל בטעינת `chain.json` **לא** מעביר לקטלוג (בלי סניף אין הזמנה מזוהה). הסניף נכתב ל-`<input type="hidden" id="cust-business">`, וקישור "שנה סניף" מופיע בכותרת ובחלון ההזמנה. `css/style.css` לא נגע — כללי `.cart-page` הקיימים חלים כי `cartView: true`.
    **מלכודת שטופלה:** `unlock()` עשוי לרוץ לפני `DOMContentLoaded` (ה-fetch מהיר יותר מפרסור העמוד), ואז `#cust-business` עדיין לא קיים; לכן הסניף נצבע שוב במאזין `DOMContentLoaded`.
    **העורך `admin/chain.html` + `chain.js`:** כרטיס מרכזי קניות (שם לעריכה ישירה + חנויות כצ׳יפים) וכרטיס מוצרים גלויים — רשימה מקובצת לפי קטגוריה עם "בחר הכל" לקטגוריה, חיפוש, ומונה; **אזהרה אדומה כשהבחירה ריקה** ("כל הקטלוג גלוי"). `server.py`: `CHAIN_FILE`, ניתוב `/api/chain` ו-`handle_save_chain` בדפוס `handle_save_promo` — ניקוי, הסרת כפילויות ותקרות, **בלי אף גנרטור** (העמוד noindex).
    גרסאות: app.js?v=45, sw cache-v38 (15 דפים). **אומת בדפדפן מקצה לקצה:** קוד שגוי נחסם · `?k=` נכנס ישירות והפרמטר נעלם מהכתובת · בחירת סניף → 22 מוצרים ו-3 קטגוריות בלבד מתוך 176 · רענון נכנס ישר עם הסניף השמור · שינוי שם חנות בקובץ החזיר את הבורר · `products: []` → כל 176 המוצרים · **פיילוד מיורט (לא נשלחה הזמנת אמת)** עם `RS-20260821-6066`, `showPrices:false` ו-`business: "קניון איילון — סניף 12"` · הודעת הוואטסאפ נפתחת ב-`סניף: ...` בלי מחירים · `aroam_customer_details` נשאר `null` · העורך round-trip מלא ו-**רק `data/chain.json` נכתב** · מובייל 375px בלי גלישה · אפס שגיאות קונסול. **רגרסיה:** `/catalog/` (176 מוצרים, "שם העסק *" עדיין חוסם שליחה), `/care/` (63 מוצרים, בוררי אספקה), `/emp-8c3f5a/` (שער, 69 מוצרים, קידומת VD), `/agent/` (PIN, עריכת מחיר 15→25 ⇒ שורה 50.00, סל ₪50, מע״מ ₪59, מודאל 800px בלי cart-page). `refresh_site.py` הורץ — היחיד שהשתנה היה `lastmod`/`sale_price_effective_date` של אתמול מול היום, כלומר אפס סחף.
    **דורש רועי:** (1) **push**; (2) להזין בעורך (`localhost/admin/chain.html`) את מרכזי הקניות והחנויות ולסמן את המוצרים הגלויים — **עד אז העמוד מציג "טרם הוגדרו סניפים"**; (3) לשקול להחליף את קוד הכניסה `3160`; (4) לא לפרסם את הכתובת בשום מקום; (5) בגיליון ההזמנות — הזמנות הרשת מזוהות בקידומת `RS-` ובעמודת "שם עסק" שמכילה `מרכז קניות — חנות`.

41. **עריכת מחיר בדף הסוכן במובייל (21.8.26):** רועי דיווח "מחקת את הפיצ'ר הכי חשוב — אין לי אפשרות לערוך מחיר בסיכום ההזמנה". **הפיצ'ר לא נמחק:** ב-Chrome ברוחב 1200px תא המחיר הוא `table-cell` ו-`readOnly:false` — כלומר בדסקטופ הוא עבד כל הזמן. **הסיבה האמיתית:** הכלל שהופך את טבלת ההזמנה לכרטיסים במובייל (`@media (max-width: 768px)` ב-style.css, מסבב 23) מסתיר `.oc-sku/.oc-cat/.oc-unit/.oc-price` — הוא נכתב כשהמחיר היה לקריאה בלבד, ולא הבחין בין קטלוג לקוחות לדף סוכן. **הזרימה החדשה חשפה את זה:** מאז סבב 38 רועי מקבל במייל קישור להצעת מחיר ופותח אותו **בטלפון** — ושם בדיוק אי אפשר היה לערוך.
    **התיקון:** `applyConfiguration` מוסיף ל-`body` את ה-class `editable-prices` לפי `CONFIG.editablePrices` (דף הסוכן בלבד), וב-`@media (max-width: 768px)` נוספה שורת מחיר משלה בכרטיס — `grid-template-areas` שלישית `"price price price"`, תווית "מחיר ליח׳:" וקלט ברוחב 100px/גובה 42px (נוח למגע, 16px נגד זום iOS מהכלל הקיים). גרסאות: app.js?v=46, style.css?v=35, sw cache-v39 (15 דפים).
    **אומת:** במסלול המובייל — `editable-prices` על ה-body, תא המחיר `flex` ברוחב 100 וגובה 42 עם התווית, עריכה 15→20 ⇒ שורה 40.00 וסל ₪270; בדסקטופ ב-Chrome אמיתי (1200px) — `table-cell`, מק"ט גלוי, אפס שינוי; `/care/` ו-`/catalog/` לא מקבלים את ה-class ולכן שורת המחיר נשארת מוסתרת אצלם במובייל כמו קודם.
    **מלכודת שחזרה:** ה-service worker הגיש HTML ישן (`app.js?v=45`) גם אחרי העלאת הגרסה — האימות הראה `bodyHasClass:false` עד ש-unregister + ניקוי caches. **בבדיקה מקומית של שינוי גרסה: לנקות SW ו-caches לפני שמסיקים שהקוד לא עובד.**

41. **בורר הסניף בריבועים (21.8.26):** רועי ביקש שמסך בחירת הסניף ייראה אחרת — ריבועים על הרקע הירוק, כיתוב ומסגרת בלבן, ואחרי בחירת מרכז ייפתחו החנויות שלו באותו עיצוב. שני ה-`<select>` וכפתור "המשך" הוחלפו בגריד `.branch-grid` של כפתורי `.branch-tile` (`aspect-ratio: 1`, מסגרת לבנה 2px, רקע שקוף, ב-hover/focus היפוך למילוי לבן עם טקסט ירוק כהה). **לחיצה אחת לכל שלב** — בחירת חנות נכנסת ישר לקטלוג; נוסף קישור "חזרה למרכזי הקניות" ומיקוד אוטומטי בריבוע הראשון לניווט במקלדת. **שני תיקוני פריסה שהתגלו באימות:** (א) במובייל `minmax(170px, 1fr)` נתן עמודה אחת וריבוע ענק לכל מרכז — נוספה מדיה `max-width: 520px` עם שתי עמודות קבועות (159×159 ב-375px, 4 מרכזים נכנסים בלי גלילה); (ב) `justify-content: center` על `#shops-gate` חתך את ראש הרשימה כשיש הרבה מרכזים ואי אפשר היה לגלול אליו — הוחלף ל-`safe center` (אומת: 12 ריבועים ⇒ הלוגו נשאר ב-top 24 והמסך נגלל). מרכז בלי חנויות מציג הודעה במקום להיתקע. **שינוי בקובץ העמוד בלבד** — אין נגיעה ב-app.js/style.css, ולכן אין העלאת גרסאות (ה-service worker הוא network-first וה-HTML מתעדכן מעצמו). אומת עם הנתונים האמיתיים של רועי: גלילות ← טומי ⇒ `מזמינים עבור: גלילות — טומי` ו-2 המוצרים הגלויים; אפס שגיאות קונסול.

42. **חנויות רחוב בבורר הסניף (21.8.26):** רועי עדכן שלרשת יש גם חנויות שאינן במרכז קניות. `data/chain.json` קיבל מערך `stores` ברמה העליונה (לצד `malls`), והמסך הראשון מציג עכשיו **שתי קבוצות**: "מרכזי קניות" (לחיצה פותחת את החנויות שבמרכז) ו"חנויות רחוב" (לחיצה נכנסת ישר לקטלוג). **הכותרות מוצגות רק כששתי הקבוצות קיימות** — כשיש רק מרכזים, המסך נראה בדיוק כמו קודם.
    **הזיהוי:** `branchLabel` מחזיר `מרכז — חנות` כשיש מרכז, ו**שם החנות בלבד** לחנות רחוב (נשמר כ-`{mall: '', store: '...'}`), ו-`branchExists` מאמת חנות רחוב מול `STREET`. השדה נשאר `business` ולכן שוב אין שינוי בגיליון או בתעודת ה-PDF.
    **תיקון אגב אורחא:** `focusFirstTile()` רץ גם ברינדור הראשון, וה-`:focus-visible` צבע את הריבוע הראשון בלבן — נראה כאילו הוא כבר נבחר. עכשיו ממקדים רק כשהמעבר נובע מפעולת משתמש (בחירת מרכז או "חזרה").
    בעורך נוסף כרטיס "חנויות רחוב" בדפוס הצ׳יפים הקיים; `handle_save_chain` מנקה גם את `stores` (ניקוי, כפילויות, תקרת 200) ומחזיר את הספירה. **אומת:** שתי הקבוצות עם כותרות · חנות רחוב ⇒ `סניף: פולו דיזנגוף` ו-`RS-20260821-6777` בפיילוד מיורט · מסלול המרכז ממשיך לתת `גלילות — בוס` · חנות רחוב שנמחקה מהקובץ מחזירה את הבורר · רק חנויות רחוב ⇒ קבוצת המרכזים נעלמת · מובייל 375px שתי עמודות בלי גלילה · round-trip בעורך · אפס שגיאות קונסול. שוב **בלי העלאת גרסאות** — app.js ו-style.css לא נגעו.

43. **"השמירה נכשלה: The string did not match the expected pattern" — שרתים מקומיים ישנים (21.8.26):** רועי ניסה לשמור בעורך הרשת וקיבל את השגיאה הזאת. **האבחון:** על המכונה רצו **ארבעה** תהליכי `server.py` במקביל (8080 מיום שני, 8081 מיום שלישי, 8082 ו-8083 מהיום). שרת ישן מגיש את קובצי ה-HTML/JS **העדכניים מהדיסק**, ולכן העמוד נראה חדש לגמרי — אבל טבלת הניתובים שלו היא מיום שני ואינה מכירה את `/api/chain`, כך שה-POST חוזר כדף שגיאה ב-HTML ו-`r.json()` נכשל. ההודעה עצמה היא הנוסח של Safari ל-`JSON.parse` על HTML. אומת ישירות: POST ל-8080/8081 החזיר 404 HTML, ול-8082/8083 החזיר JSON תקין.
    **למה הוא נחת דווקא על הישן:** `עריכת מחירונים.command` בחר "את הפורט הראשון בטווח 8080-8090 שתפוס" — כלומר תמיד 8080, השרת הכי ישן. `start_catalog.command` מנגד תמיד פתח שרת **נוסף**, וכך הם נערמו.
    **התיקון:** endpoint חדש `GET /api/ping` (מחזיר `{"ok":true,"app":"aroam","version":2}`), ושני קובצי ה-`.command` בוחרים עכשיו שרת לפי **תשובה ל-ping** ולא לפי "הפורט תפוס" — שרת ישן פשוט מדולג ונפתח אחד חדש. `start_catalog.command` גם מתחבר לשרת תקין קיים במקום לפתוח עוד אחד. בנוסף `admin/chain.js` מזהה תשובה שאינה JSON ומציג "השרת המקומי ישן... הפעל מחדש את start_catalog.command" במקום השגיאה של הדפדפן.
    **לקח:** אחרי כל שינוי ב-`server.py` צריך להפעיל מחדש את השרת. עמוד שנראה עדכני אינו ראיה לכך שהשרת עדכני — הקבצים הסטטיים נקראים מהדיסק בכל בקשה, הקוד של Python לא.

44. **שם לתצוגה ומחיר לרשת (21.8.26):** רועי ביקש (א) לערוך את שמות המוצרים כפי שהם מוצגים לחנויות, ו-(ב) לקבוע מחיר לרשת שבעלי החנויות **לא רואים**, כדי שיראה כמה יצאה ההזמנה כשהיא מגיעה אליו (מחיר שונה מזה של הסוכן).
    **המימוש:** `data/chain.json` קיבל `overrides: {מזהה: {name, price}}`, ו-`applyProductFilter` ב-app.js מחיל אותם על `PRODUCTS` אחרי הסינון. השם מחליף את `product.name` בכל מקום שקורא אותו (כרטיס, חיפוש, טבלת ההזמנה, ההודעה, הפיילוד ותעודת ה-PDF); המחיר נכנס ל-`product.price`, וכיוון ש-`showPrices` כבוי הוא **לא מרונדר בשום מקום** — `fillOrderTable` בכלל לא יוצר את תאי המחיר, ו-`buildMessage` מגדר את המחיר באותו תנאי. לעומת זאת `getCartItemsData` כן משתמש בו, ולכן `totalPrice` בפיילוד הוא **סכום לפי מחירי הרשת** ומגיע לגיליון ולתעודה. שדה ריק = נופלים למחיר/שם של הקטלוג הראשי.
    **בעורך:** לכל מוצר שסומן נפתחות בשורה שתי תיבות — "שם לתצוגה" ו"מחיר ₪" — ונוסף מסנן "הצג רק נבחרים" לסקירה מרוכזת. **שורת המוצר הומרה מ-`<label>` ל-`<div>`** עם `label` פנימי סביב הסימון בלבד, אחרת לחיצה על תיבת הטקסט הייתה מבטלת את סימון המוצר. השרת שומר רק רשומות עם שם או מחיר תקין, והעורך שולח רק overrides של מוצרים שנבחרו.
    **מגבלה שנמסרה לרועי:** `data/chain.json` הוא קובץ שהדפדפן מוריד, ולכן המחירים מוסתרים מהמסך אבל **לא מוגנים ממי שיפתח את הקובץ**. הסתרה אמיתית מחייבת שהמחירים לא יגיעו לאתר בכלל (למשל חישוב בסקריפט שבענן מול טאב בגיליון) — לא בוצע.
    גרסאות: app.js?v=47, sw cache-v40. **אומת:** אבי004 (12₪ בקטלוג) ואבי005 (6₪) קיבלו 18.90 ו-7.25 ⇒ הכרטיס הציג את השם החדש, אפס `₪` בעמוד, והפיילוד המיורט נתן 37.80 + 21.75 = **59.55** עם `showPrices:false`; הודעת הוואטסאפ בלי מחירים; לחיצה על שדה השם לא מבטלת סימון; round-trip בעורך. **רגרסיה:** `/catalog/` ו-`/agent/` מציגים את אבי004 בשם ובמחיר של הקטלוג הראשי (12₪) — ההחלה מגודרת ב-`allowedProductsSource`. נתוני הבדיקה נוקו.

45. **ברירת מחדל של תצוגת שורות + מסנן נזכר בעורך (21.8.26):** רועי ביקש נוחות עריכה. **(א)** מתג רשת/רשימה ב-`components.js` קיבל ברירת מחדל פר עמוד דרך `CATALOG_CONFIG.defaultView`, ובעמוד הרשת היא `'list'` — הזמנה חוזרת לסניף נוחה יותר בשורות. **(ב)** מפתח ההעדפה הוסב ל-`aroam_catalog_view_<נתיב>` (כמו `CART_KEY`), אחרת העדפת התצוגה בקטלוג העסקי הייתה גוררת את עמוד הרשת וברירת המחדל לא הייתה חלה בכלל. תופעת לוואי מכוונת: העדפה שמורה קיימת מתאפסת פעם אחת. **(ג)** המסנן "הצג רק נבחרים" בעורך נזכר בין ביקורים (`aroam_chain_only_selected`).
    **אומת:** עמוד הרשת נפתח ב-`list-view` והמתג מסומן על "רשימה"; `/catalog/` עדיין נפתח ב-grid והמתג עובד ונשמר תחת המפתח החדש; בעורך 181 שורות ⇒ 27 בסימון המסנן, והמסנן נשאר מסומן אחרי רענון עם 27 השורות ושני השמות לתצוגה של רועי. sw cache-v41. `app.js`/`style.css` לא נגעו.

46. **כפתור "חזור" של הדפדפן בבורר הסניף (21.8.26):** רועי דיווח ש"חזור" מוציא מהאתר במקום להחזיר שלב אחד — למשל אחרי בחירה שגויה במרכז הקניות. הבורר היה מסך אחד עם מצב ב-JS ובלי אף ערך היסטוריה, ולכן "חזור" ניווט מחוץ לעמוד.
    **המימוש:** כניסה למרכז דוחפת `pushState({shopsStep:'stores', mall})`, ובחירת סניף מותירה **בדיוק ערך אחד מעל מסך הבורר** — `replaceState` כשמגיעים ממסך החנויות ו-`pushState` כשבוחרים חנות רחוב ישירות. `popstate` מרנדר את המצב שנשמר: `stores` ← חנויות אותו מרכז, אין מצב ← רשימת הסניפים. הקישור "חזרה לרשימת הסניפים" קורא `history.back()` ולא מרנדר בעצמו, אחרת הכפתור בעמוד וכפתור הדפדפן היו מתארים מצבים שונים.
    **השער מוסתר במקום להימחק** (`hideGate`/`reopenGateAtMalls` במקום `gate.remove()`), כדי ש"חזור" מהקטלוג יוכל להחזיר את הבורר.
    **התנגשות שטופלה:** גם `app.js` מאזין ל-`popstate` (סגירת תצוגת הסל). המאזין בעמוד נרשם ראשון ויוצא מוקדם כש-`.cart-page` פעיל, ולכן "חזור" בתוך הסל סוגר את הסל ואינו פותח את בורר הסניף.
    **באג נסתר שתוקן אגב אורחא:** ציור הסניף ב-`DOMContentLoaded` היה מגודר ב-`b.mall && b.store`, ולכן **חנות רחוב** (שנשמרת עם `mall` ריק) לא נצבעה כשה-fetch הקדים את פרסור העמוד — `cust-business` היה נשאר ריק וההזמנה יוצאת בלי סניף. התנאי הוא עכשיו `b.store` בלבד.
    **אומת:** חזור משלב החנויות ← רשימת הסניפים עם חנויות הרחוב חזרה גלויות; חזור מהקטלוג ← הבורר (גם במסלול המרכז וגם בחנות רחוב); חזור בתוך הסל ← הסל נסגר, הבורר לא נפתח והסניף נשמר; הקישור בעמוד מתנהג כמו כפתור הדפדפן; מובייל 375px בלי גלישה; אפס שגיאות קונסול. שינוי בקובץ העמוד בלבד — בלי העלאת גרסאות.

47. **שם המזמין בעמוד הרשת (21.8.26):** רועי ביקש שמי שממלא את ההזמנה יזין גם את שמו. עד כה `customerMode: 'branch'` ויתר על כל שדות החובה, כי הסניף הספיק לזיהוי.
    **מה נוסף:** שדה `cust-contact` ("שם המזמין", חובה) לצד שדה ההערות בטופס, בגריד שנשבר לעמודה אחת במובייל. ב-`validateCustomerDetails` מצב `branch` דורש עכשיו `contact` בלבד (הסניף מובטח מהשער), עם toast וסימון אדום כמו בשאר הקטלוגים. ב-`buildMessage` התווית במצב `branch` היא **`מזמין:`** ולא `איש קשר:`, כי ה"עסק" בשורה שמעליה הוא הסניף.
    **השם נזכר במכשיר** במפתח של העמוד (`aroam_shops_orderer`, נשמר ונטען בסקריפט של העמוד) ולא ב-`aroam_customer_details` המשותף — אחרת שם המזמין והסניף היו מודלפים לטופס של הקטלוג העסקי.
    **הפיילוד לא השתנה מבנית** — `customer.contact` פשוט מפסיק להיות ריק, ולכן הוא נוחת בעמודת איש הקשר בגיליון ובתעודת ה-PDF בלי שום שינוי בענן.
    גרסאות: app.js?v=48, sw cache-v42. **אומת:** שליחה בלי שם נחסמה עם "נא למלא את שם המזמין" ומיקוד אדום; עם שם ⇒ `סניף: אמור` / `מזמין: דנה כהן` בהודעה ו-`contact: "דנה כהן"` בפיילוד המיורט; השם מולא מראש אחרי רענון; `aroam_customer_details` נשאר `null`; דסקטופ שתי עמודות ומובייל 375px עמודה אחת בלי גלישה. **רגרסיה:** `/catalog/` ("נא למלא שם עסק וטלפון", ובהודעה `איש קשר:`), `/care/` ("נא למלא שם מלא וטלפון", ובהודעה `שם:`).

## העדפות של רועי

- תשובות קצרות וענייניות, בעברית.
- בלי אימוג'ים בעיצוב האתר; כפתורים בסגנון האתר הקיים.
- לשמור על עיצוב פסטל ירוק; שינויים עיצוביים — רק עם נקודת שחזור git לפני.
- מוצרים בלי מחיר/תיאור זה מכוון — לא "לתקן".
