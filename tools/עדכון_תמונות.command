#!/bin/bash
# אחדת תמונות מוצרים - לחיצה כפולה מריצה את התהליך
cd "$(dirname "$0")"
echo "מתחיל עיבוד תמונות... (זה יכול לקחת דקה-שתיים)"
python3 process_images.py
echo ""
read -p "סיימנו! אפשר לסגור את החלון (Enter ליציאה)"
